// This file was automatically generated by gyb
// DO NOT EDIT!
// Compiled from community_feedback.proto

#import "GEORPMerchantLookupFeedback.h"
#import <ProtocolBuffer/PBConstants.h>
#import <ProtocolBuffer/PBHashUtil.h>
#import <ProtocolBuffer/PBDataReader.h>
#import <os/lock_private.h>

#import <GeoServices/GEORPMerchantLookupContext.h>
#import <GeoServices/GEORPMerchantLookupCorrections.h>

#if !__has_feature(objc_arc)
# error This generated file depends on ARC but it is not enabled
#endif

@implementation GEORPMerchantLookupFeedback

- (instancetype)init
{
    self = [super init];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (instancetype)initWithData:(NSData *)data
{
    self = [super initWithData:data];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (void)_readContext {
    if (_flags.read_context) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_context) {
            static const uint32_t tags[] = {1, 0};
            GEORPMerchantLookupFeedbackReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasContext
{
    [self _readContext];
    return _context != nil;
}
- (GEORPMerchantLookupContext *)context
{
    [self _readContext];
    return _context;
}
- (void)setContext:(GEORPMerchantLookupContext *)v
{
    _flags.read_context = 1;
    _flags.wrote_context = 1;
    _context = v;
}

- (void)_readCorrections {
    if (_flags.read_corrections) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_corrections) {
            static const uint32_t tags[] = {2, 0};
            GEORPMerchantLookupFeedbackReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasCorrections
{
    [self _readCorrections];
    return _corrections != nil;
}
- (GEORPMerchantLookupCorrections *)corrections
{
    [self _readCorrections];
    return _corrections;
}
- (void)setCorrections:(GEORPMerchantLookupCorrections *)v
{
    _flags.read_corrections = 1;
    _flags.wrote_corrections = 1;
    _corrections = v;
}


- (NSString *)description
{
    return [NSString stringWithFormat:@"%@ %@", [super description], [self dictionaryRepresentation]];
}

- (NSDictionary *)dictionaryRepresentation
{
    [self readAll:YES];
    NSMutableDictionary *dict = [NSMutableDictionary dictionary];
    {
        GEORPMerchantLookupContext * value = self.context;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"context"];
        }
    }

    {
        GEORPMerchantLookupCorrections * value = self.corrections;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"corrections"];
        }
    }


    if (_unknownFields)
        [dict setObject:[_unknownFields dictionaryRepresentation] forKey:@"Unknown Fields"];

    return dict;
}


#define TAG_PARSE_ALL_RECURSIVE     (UINT32_MAX)
#define TAG_PARSE_ALL_NON_RECURSIVE (UINT32_MAX-1)
#define TAG_PARSE_INITIAL           (UINT32_MAX-2)
#define TAG_UNKNOWN_FIELDS          (UINT32_MAX-3)

- (void)readAll:(BOOL)recursively {
    os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (reader == nil) return;
    @synchronized(reader) {
        static const uint32_t recursiveTag[] = {TAG_PARSE_ALL_RECURSIVE, 0};
        static const uint32_t nonRecursiveTag[] = {TAG_PARSE_ALL_NON_RECURSIVE, 0};
        GEORPMerchantLookupFeedbackReadSpecified(self, reader, recursively ? recursiveTag : nonRecursiveTag);
    }
}
BOOL GEORPMerchantLookupFeedbackReadAllFrom(__unsafe_unretained GEORPMerchantLookupFeedback *self, __unsafe_unretained PBDataReader *reader, BOOL recursively) {
    if (self->_readerMarkLength != 0) {
        [NSException raise:NSInvalidArgumentException format:@"GEORPMerchantLookupFeedbackReadAllFrom can only be called once per object"];
        return NO;
    }

    if (reader.position > UINT32_MAX || reader.length > UINT32_MAX) {
        os_log_fault(OS_LOG_DEFAULT, "Cannot decode data with position/length > UINT32_MAX");
        return NO;
    }

    static const uint32_t recursiveTag[] = {TAG_PARSE_INITIAL, TAG_PARSE_ALL_RECURSIVE, 0};
    static const uint32_t initialTag[] = {TAG_PARSE_INITIAL, 0};

    @synchronized(reader) {
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = reader;
        os_unfair_lock_unlock(&(self->_readerLock));

        self->_readerMarkPos = (uint32_t)reader.position;
        self->_readerMarkLength = (uint32_t)reader.length;
        BOOL result = GEORPMerchantLookupFeedbackReadSpecified(self, reader, recursively ? recursiveTag : initialTag);
        return result;
    }
}

BOOL GEORPMerchantLookupFeedbackReadFrom(__unsafe_unretained GEORPMerchantLookupFeedback *self, __unsafe_unretained PBDataReader *reader) {
    @synchronized(reader) {
        return GEORPMerchantLookupFeedbackReadAllFrom(self, reader, NO);
    }
}

BOOL GEORPMerchantLookupFeedbackReadSpecified(__unsafe_unretained GEORPMerchantLookupFeedback *self, __unsafe_unretained PBDataReader *reader, const uint32_t *tags) {
    if (reader == nil) {
        return NO;
    }
    reader->_length = (NSUInteger)self->_readerMarkLength;
    [reader seekToOffset:(NSUInteger)self->_readerMarkPos];
    uint32_t readLazyCount = 0
        + self->_flags.read_context
        + self->_flags.read_corrections
        + self->_flags.read_unknownFields
        ;
    #define requiredLazyFieldCount 1 // objc_msg_parse_all_threshold = 0.8

    BOOL readAll = NO;
    __attribute__((unused)) BOOL recursively = NO;
    BOOL shouldParseUnknown = NO;
    BOOL isInitial = NO;
    size_t tagsRemaining = 0;
    for (; tags[tagsRemaining] != 0; ++tagsRemaining) {
        switch (tags[tagsRemaining]) {
            case TAG_PARSE_ALL_RECURSIVE:
                recursively = YES;
                readAll = YES;
                shouldParseUnknown = YES;
                break;
            case TAG_PARSE_ALL_NON_RECURSIVE:
                readAll = YES;
                shouldParseUnknown = YES;
                break;
            case TAG_PARSE_INITIAL:
                isInitial = YES;
                break;
            case TAG_UNKNOWN_FIELDS:
                shouldParseUnknown = YES;
                ++readLazyCount;
                break;
            default:
                ++readLazyCount;
                break;
            }
    }
    if (!readAll && readLazyCount >= requiredLazyFieldCount) {
        readAll = YES;
        shouldParseUnknown = YES;
    }
    const BOOL stopWhenNoMoreTagsRemaining = !(readAll || shouldParseUnknown);
    if (readAll || isInitial) tags = NULL;

    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }
        
        BOOL shouldParseTag = readAll;
        for (size_t i = 0; tags != NULL && !shouldParseTag && tags[i] != 0; ++i) {
            shouldParseTag = (tags[i] == tag);
        }

        switch (tag) {

            case 1 /* context */:
            {
                if (!shouldParseTag || self->_flags.read_context) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_context) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEORPMerchantLookupContext *new_context = [[GEORPMerchantLookupContext alloc] init];
                self->_context = new_context;
                PBDataReaderMark mark_context;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_context);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEORPMerchantLookupContextReadAllFrom(new_context, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_context);

                --tagsRemaining;
            }
            break;
            case 2 /* corrections */:
            {
                if (!shouldParseTag || self->_flags.read_corrections) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_corrections) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEORPMerchantLookupCorrections *new_corrections = [[GEORPMerchantLookupCorrections alloc] init];
                self->_corrections = new_corrections;
                PBDataReaderMark mark_corrections;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_corrections);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEORPMerchantLookupCorrectionsReadAllFrom(new_corrections, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_corrections);

                --tagsRemaining;
            }
            break;
            default:
                if (!shouldParseUnknown || self->_flags.read_unknownFields) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                if (!self->_unknownFields)
                    self->_unknownFields = [[PBUnknownFields alloc] init];
                if (!PBUnknownFieldAdd(self->_unknownFields, tag, aType, reader))
                    return NO;
                break;
        }
        if (stopWhenNoMoreTagsRemaining && tagsRemaining == 0) {
            break;
        }
    }

    if (shouldParseUnknown) {
        self->_flags.read_unknownFields = YES;
    }

    if (readAll) {
        if (recursively) {
            [self->_context readAll:YES];
            [self->_corrections readAll:YES];
        }
        self->_flags.read_context = YES;
        self->_flags.read_corrections = YES;
    } else {
        for (size_t i = 0; tags != NULL && tags[i] != 0; ++i) {
            switch (tags[i]) {
                case 1 /* context */:
                    self->_flags.read_context = YES;
                    break;
                case 2 /* corrections */:
                    self->_flags.read_corrections = YES;
                    break;
            }
        }
    }

    #undef requiredLazyFieldCount
    if (readAll) {
        // reader is __unsafe_unretained
        const BOOL hadError = PBReaderHasError(reader);
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = nil;
        os_unfair_lock_unlock(&(self->_readerLock));
        return !hadError;
    }
    return !PBReaderHasError(reader);
}

+ (BOOL)isValid:(__unsafe_unretained PBDataReader *)reader
{
    return GEORPMerchantLookupFeedbackIsValid(reader);
}

BOOL GEORPMerchantLookupFeedbackIsValid(__unsafe_unretained PBDataReader *reader) {
    PBDataReaderMark resetMark;
    resetMark.pos = reader.position;
    resetMark.length = reader.length;
    #define returnValue(X) do { \
        const BOOL v = X; \
        PBReaderRecallMark(reader, &resetMark); \
        return v; \
    } while(0)
    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }

        switch (tag) {

            case 1 /* context */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_context;
                    if (!PBReaderPlaceMark(reader, &mark_context))
                        returnValue(NO);
                    if (!GEORPMerchantLookupContextIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_context);
                }
                break;
            case 2 /* corrections */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_corrections;
                    if (!PBReaderPlaceMark(reader, &mark_corrections))
                        returnValue(NO);
                    if (!GEORPMerchantLookupCorrectionsIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_corrections);
                }
                break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
        }
    }
    returnValue(!PBReaderHasError(reader));
    #undef returnValue
}

- (BOOL)readFrom:(PBDataReader *)reader
{
    return GEORPMerchantLookupFeedbackReadAllFrom(self, reader, NO);
}

- (void)writeTo:(PBDataWriter *)writer
{
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            if (!GEORPMerchantLookupFeedbackIsDirty(self)) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                uint32_t length = (_readerMarkLength - _readerMarkPos);
                NSData *data = [reader readBytes:length];
                [writer writeData:data];
                return;
            }
        }
    }

    [self readAll:NO];

    /* context */
    {
        if (self->_context != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_context, 1);
        }
    }

    /* corrections */
    {
        if (self->_corrections != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_corrections, 2);
        }
    }
    /* Unknown Fields */
    {
        [_unknownFields writeTo:writer];
    }
}

BOOL GEORPMerchantLookupFeedbackIsDirty(__unsafe_unretained GEORPMerchantLookupFeedback *self)
{
    if (self == nil) {
        return NO;
    }
    
    BOOL dirty = NO
        || self->_flags.wrote_context
        || (self->_flags.read_context && GEORPMerchantLookupContextIsDirty(self->_context))
        || self->_flags.wrote_corrections
        || self->_flags.read_corrections
        || self->_flags.wrote_unknownFields
    ;


    return dirty;
}


- (void)copyTo:(GEORPMerchantLookupFeedback *)other
{
    [self readAll:NO];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    other->_reader = _reader;
    other->_readerMarkPos = _readerMarkPos;
    other->_readerMarkLength = _readerMarkLength;
    os_unfair_lock_unlock(&_readerLock);
    if (_context)
    {
        other.context = _context;
    }
    if (_corrections)
    {
        other.corrections = _corrections;
    }
}

- (id)copyWithZone:(NSZone *)zone
{
    GEORPMerchantLookupFeedback *copy = [[[self class] allocWithZone:zone] init];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            BOOL wroteAny = NO
                || _flags.wrote_context
                || _flags.wrote_corrections
                || _flags.wrote_unknownFields
            ;
            if (!wroteAny) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                GEORPMerchantLookupFeedbackReadAllFrom(copy, reader, NO);
                return copy;
            }
            [self readAll:NO];
        }
    }
    copy->_context = [_context copyWithZone:zone];
    copy->_corrections = [_corrections copyWithZone:zone];
    copy->_unknownFields = _unknownFields;
    return copy;
}


- (BOOL)isEqual:(id)object
{
    GEORPMerchantLookupFeedback *other = (GEORPMerchantLookupFeedback *)object;
    if (![other isMemberOfClass:[self class]]) {
        return NO;
    }
    [self readAll:YES];
    [other readAll:YES];


    return YES
    &&
    ((!self->_context && !other->_context) || [self->_context isEqual:other->_context])
    &&
    ((!self->_corrections && !other->_corrections) || [self->_corrections isEqual:other->_corrections])
    ;
}

- (NSUInteger)hash
{
    [self readAll:YES];
    NSUInteger value = 0;

    return value
    ^
    [self->_context hash]
    ^
    [self->_corrections hash]
    ;
}

- (void)mergeFrom:(GEORPMerchantLookupFeedback *)other
{
    [other readAll:NO];
    if (self->_context && other->_context)
    {
        [self->_context mergeFrom:other->_context];
    }
    else if (!self->_context && other->_context)
    {
        [self setContext:other->_context];
    }
    if (self->_corrections && other->_corrections)
    {
        [self->_corrections mergeFrom:other->_corrections];
    }
    else if (!self->_corrections && other->_corrections)
    {
        [self setCorrections:other->_corrections];
    }
}

- (PBUnknownFields *)unknownFields
{
    if (_flags.read_unknownFields) return _unknownFields;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return _unknownFields;
    @synchronized(reader) {
        if (!_flags.read_unknownFields) {
            static const uint32_t tags[] = {TAG_UNKNOWN_FIELDS, 0};
            GEORPMerchantLookupFeedbackReadSpecified(self, reader, tags);
        }
    }
    return _unknownFields;
}
- (void)clearUnknownFields:(BOOL)recursively
{
    _flags.read_unknownFields = 1;
    _flags.wrote_unknownFields = 1;
    _unknownFields = nil;
    if (!recursively) {
        return;
    }
    [self readAll:NO];
}

@end


