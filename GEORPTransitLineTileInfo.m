// This file was automatically generated by gyb
// DO NOT EDIT!
// Compiled from reportaproblem.proto

#import "GEORPTransitLineTileInfo.h"
#import <ProtocolBuffer/PBConstants.h>
#import <ProtocolBuffer/PBHashUtil.h>
#import <ProtocolBuffer/PBDataReader.h>
#import <os/lock_private.h>

#if !__has_feature(objc_arc)
# error This generated file depends on ARC but it is not enabled
#endif

@implementation GEORPTransitLineTileInfo

- (instancetype)init
{
    self = [super init];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (instancetype)initWithData:(NSData *)data
{
    self = [super initWithData:data];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (uint64_t)transitLineMuid
{
    return _transitLineMuid;
}
- (void)setTransitLineMuid:(uint64_t)v
{
    _flags.wrote_transitLineMuid = 1;
    _flags.has_transitLineMuid = 1;
    _transitLineMuid = v;
}
- (void)setHasTransitLineMuid:(BOOL)f
{
    _flags.wrote_transitLineMuid = 1;
    _flags.has_transitLineMuid = f ? 1 : 0;
}
- (BOOL)hasTransitLineMuid
{
    return (BOOL)_flags.has_transitLineMuid;
}

- (void)_readTransitLineName {
    if (_flags.read_transitLineName) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_transitLineName) {
            static const uint32_t tags[] = {2, 0};
            GEORPTransitLineTileInfoReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasTransitLineName
{
    [self _readTransitLineName];
    return _transitLineName != nil;
}
- (NSString *)transitLineName
{
    [self _readTransitLineName];
    return _transitLineName;
}
- (void)setTransitLineName:(NSString *)v
{
    _flags.read_transitLineName = 1;
    _flags.wrote_transitLineName = 1;
    _transitLineName = v;
}

- (void)_readTransitSystemName {
    if (_flags.read_transitSystemName) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_transitSystemName) {
            static const uint32_t tags[] = {3, 0};
            GEORPTransitLineTileInfoReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasTransitSystemName
{
    [self _readTransitSystemName];
    return _transitSystemName != nil;
}
- (NSString *)transitSystemName
{
    [self _readTransitSystemName];
    return _transitSystemName;
}
- (void)setTransitSystemName:(NSString *)v
{
    _flags.read_transitSystemName = 1;
    _flags.wrote_transitSystemName = 1;
    _transitSystemName = v;
}


- (NSString *)description
{
    return [NSString stringWithFormat:@"%@ %@", [super description], [self dictionaryRepresentation]];
}

- (NSDictionary *)dictionaryRepresentation
{
    [self readAll:YES];
    NSMutableDictionary *dict = [NSMutableDictionary dictionary];
    if (self->_flags.has_transitLineMuid)
    {
        [dict setObject:@(self->_transitLineMuid) forKey:@"transit_line_muid"];
    }

    {
        NSString * value = self.transitLineName;
        if (value) {
            [dict setObject:value forKey:@"transit_line_name"];
        }
    }

    {
        NSString * value = self.transitSystemName;
        if (value) {
            [dict setObject:value forKey:@"transit_system_name"];
        }
    }



    return dict;
}


#define TAG_PARSE_ALL_RECURSIVE     (UINT32_MAX)
#define TAG_PARSE_ALL_NON_RECURSIVE (UINT32_MAX-1)
#define TAG_PARSE_INITIAL           (UINT32_MAX-2)
#define TAG_UNKNOWN_FIELDS          (UINT32_MAX-3)

- (void)readAll:(BOOL)recursively {
    os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (reader == nil) return;
    @synchronized(reader) {
        static const uint32_t recursiveTag[] = {TAG_PARSE_ALL_RECURSIVE, 0};
        static const uint32_t nonRecursiveTag[] = {TAG_PARSE_ALL_NON_RECURSIVE, 0};
        GEORPTransitLineTileInfoReadSpecified(self, reader, recursively ? recursiveTag : nonRecursiveTag);
    }
}
BOOL GEORPTransitLineTileInfoReadAllFrom(__unsafe_unretained GEORPTransitLineTileInfo *self, __unsafe_unretained PBDataReader *reader, BOOL recursively) {
    if (self->_readerMarkLength != 0) {
        [NSException raise:NSInvalidArgumentException format:@"GEORPTransitLineTileInfoReadAllFrom can only be called once per object"];
        return NO;
    }

    if (reader.position > UINT32_MAX || reader.length > UINT32_MAX) {
        os_log_fault(OS_LOG_DEFAULT, "Cannot decode data with position/length > UINT32_MAX");
        return NO;
    }

    static const uint32_t recursiveTag[] = {TAG_PARSE_INITIAL, TAG_PARSE_ALL_RECURSIVE, 0};
    static const uint32_t initialTag[] = {TAG_PARSE_INITIAL, 0};

    @synchronized(reader) {
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = reader;
        os_unfair_lock_unlock(&(self->_readerLock));

        self->_readerMarkPos = (uint32_t)reader.position;
        self->_readerMarkLength = (uint32_t)reader.length;
        BOOL result = GEORPTransitLineTileInfoReadSpecified(self, reader, recursively ? recursiveTag : initialTag);
        return result;
    }
}

BOOL GEORPTransitLineTileInfoReadFrom(__unsafe_unretained GEORPTransitLineTileInfo *self, __unsafe_unretained PBDataReader *reader) {
    @synchronized(reader) {
        return GEORPTransitLineTileInfoReadAllFrom(self, reader, NO);
    }
}

BOOL GEORPTransitLineTileInfoReadSpecified(__unsafe_unretained GEORPTransitLineTileInfo *self, __unsafe_unretained PBDataReader *reader, const uint32_t *tags) {
    if (reader == nil) {
        return NO;
    }
    reader->_length = (NSUInteger)self->_readerMarkLength;
    [reader seekToOffset:(NSUInteger)self->_readerMarkPos];
    uint32_t readLazyCount = 0
        + self->_flags.read_transitLineName
        + self->_flags.read_transitSystemName
        ;
    #define requiredLazyFieldCount 1 // objc_msg_parse_all_threshold = 0.8

    BOOL readAll = NO;
    __attribute__((unused)) BOOL recursively = NO;
    BOOL isInitial = NO;
    size_t tagsRemaining = 0;
    for (; tags[tagsRemaining] != 0; ++tagsRemaining) {
        switch (tags[tagsRemaining]) {
            case TAG_PARSE_ALL_RECURSIVE:
                recursively = YES;
                readAll = YES;
                break;
            case TAG_PARSE_ALL_NON_RECURSIVE:
                readAll = YES;
                break;
            case TAG_PARSE_INITIAL:
                isInitial = YES;
                break;
            default:
                ++readLazyCount;
                break;
            }
    }
    if (!readAll && readLazyCount >= requiredLazyFieldCount) {
        readAll = YES;
    }
    const BOOL stopWhenNoMoreTagsRemaining = !readAll;
    if (readAll || isInitial) tags = NULL;

    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }
        
        BOOL shouldParseTag = readAll;
        for (size_t i = 0; tags != NULL && !shouldParseTag && tags[i] != 0; ++i) {
            shouldParseTag = (tags[i] == tag);
        }

        switch (tag) {

            case 1 /* transitLineMuid */:
            {
                if (!isInitial) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                self->_flags.has_transitLineMuid = YES;
                self->_transitLineMuid = PBReaderReadUint64(reader);

            }
            break;
            case 2 /* transitLineName */:
            {
                if (!shouldParseTag || self->_flags.read_transitLineName) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_transitLineName) {
                        --tagsRemaining;
                    }
                    break;
                }
                NSString * new_transitLineName = PBReaderReadString(reader);
                self->_transitLineName = new_transitLineName;

                --tagsRemaining;
            }
            break;
            case 3 /* transitSystemName */:
            {
                if (!shouldParseTag || self->_flags.read_transitSystemName) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_transitSystemName) {
                        --tagsRemaining;
                    }
                    break;
                }
                NSString * new_transitSystemName = PBReaderReadString(reader);
                self->_transitSystemName = new_transitSystemName;

                --tagsRemaining;
            }
            break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    return NO;
                break;
        }
        if (stopWhenNoMoreTagsRemaining && tagsRemaining == 0) {
            break;
        }
    }


    if (readAll) {
        if (recursively) {
        }
        self->_flags.read_transitLineName = YES;
        self->_flags.read_transitSystemName = YES;
    } else {
        for (size_t i = 0; tags != NULL && tags[i] != 0; ++i) {
            switch (tags[i]) {
                case 2 /* transitLineName */:
                    self->_flags.read_transitLineName = YES;
                    break;
                case 3 /* transitSystemName */:
                    self->_flags.read_transitSystemName = YES;
                    break;
            }
        }
    }

    #undef requiredLazyFieldCount
    if (readAll) {
        // reader is __unsafe_unretained
        const BOOL hadError = PBReaderHasError(reader);
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = nil;
        os_unfair_lock_unlock(&(self->_readerLock));
        return !hadError;
    }
    return !PBReaderHasError(reader);
}

+ (BOOL)isValid:(__unsafe_unretained PBDataReader *)reader
{
    return GEORPTransitLineTileInfoIsValid(reader);
}

BOOL GEORPTransitLineTileInfoIsValid(__unsafe_unretained PBDataReader *reader) {
    PBDataReaderMark resetMark;
    resetMark.pos = reader.position;
    resetMark.length = reader.length;
    #define returnValue(X) do { \
        const BOOL v = X; \
        PBReaderRecallMark(reader, &resetMark); \
        return v; \
    } while(0)
    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }

        switch (tag) {

            case 1 /* transitLineMuid */:
                if (TYPE_VARINT != aType)
                    returnValue(NO);
                PBReaderReadUint64(reader);
                break;
            case 2 /* transitLineName */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
                break;
            case 3 /* transitSystemName */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
                break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
        }
    }
    returnValue(!PBReaderHasError(reader));
    #undef returnValue
}

- (BOOL)readFrom:(PBDataReader *)reader
{
    return GEORPTransitLineTileInfoReadAllFrom(self, reader, NO);
}

- (void)writeTo:(PBDataWriter *)writer
{
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            if (!GEORPTransitLineTileInfoIsDirty(self)) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                uint32_t length = (_readerMarkLength - _readerMarkPos);
                NSData *data = [reader readBytes:length];
                [writer writeData:data];
                return;
            }
        }
    }

    [self readAll:NO];

    /* transitLineMuid */
    {
        if (self->_flags.has_transitLineMuid)
        {
            PBDataWriterWriteUint64Field(writer, self->_transitLineMuid, 1);
        }
    }

    /* transitLineName */
    {
        if (self->_transitLineName)
        {
            PBDataWriterWriteStringField(writer, self->_transitLineName, 2);
        }
    }

    /* transitSystemName */
    {
        if (self->_transitSystemName)
        {
            PBDataWriterWriteStringField(writer, self->_transitSystemName, 3);
        }
    }
}

BOOL GEORPTransitLineTileInfoIsDirty(__unsafe_unretained GEORPTransitLineTileInfo *self)
{
    if (self == nil) {
        return NO;
    }
    
    BOOL dirty = NO
        || self->_flags.wrote_transitLineMuid
        || self->_flags.wrote_transitLineName
        || self->_flags.read_transitLineName
        || self->_flags.wrote_transitSystemName
        || self->_flags.read_transitSystemName
    ;


    return dirty;
}


- (void)copyTo:(GEORPTransitLineTileInfo *)other
{
    [self readAll:NO];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    other->_reader = _reader;
    other->_readerMarkPos = _readerMarkPos;
    other->_readerMarkLength = _readerMarkLength;
    os_unfair_lock_unlock(&_readerLock);
    if (self->_flags.has_transitLineMuid)
    {
        other->_transitLineMuid = _transitLineMuid;
        other->_flags.has_transitLineMuid = YES;
    }
    if (_transitLineName)
    {
        other.transitLineName = _transitLineName;
    }
    if (_transitSystemName)
    {
        other.transitSystemName = _transitSystemName;
    }
}

- (id)copyWithZone:(NSZone *)zone
{
    GEORPTransitLineTileInfo *copy = [[[self class] allocWithZone:zone] init];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            BOOL wroteAny = NO
                || _flags.wrote_transitLineMuid
                || _flags.wrote_transitLineName
                || _flags.wrote_transitSystemName
            ;
            if (!wroteAny) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                GEORPTransitLineTileInfoReadAllFrom(copy, reader, NO);
                return copy;
            }
            [self readAll:NO];
        }
    }
    if (self->_flags.has_transitLineMuid)
    {
        copy->_transitLineMuid = _transitLineMuid;
        copy->_flags.has_transitLineMuid = YES;
    }
    copy->_transitLineName = [_transitLineName copyWithZone:zone];
    copy->_transitSystemName = [_transitSystemName copyWithZone:zone];
    return copy;
}


- (BOOL)isEqual:(id)object
{
    GEORPTransitLineTileInfo *other = (GEORPTransitLineTileInfo *)object;
    if (![other isMemberOfClass:[self class]]) {
        return NO;
    }
    [self readAll:YES];
    [other readAll:YES];


    return YES
    &&
    ((self->_flags.has_transitLineMuid && other->_flags.has_transitLineMuid && self->_transitLineMuid == other->_transitLineMuid) || (!self->_flags.has_transitLineMuid && !other->_flags.has_transitLineMuid))
    &&
    ((!self->_transitLineName && !other->_transitLineName) || [self->_transitLineName isEqual:other->_transitLineName])
    &&
    ((!self->_transitSystemName && !other->_transitSystemName) || [self->_transitSystemName isEqual:other->_transitSystemName])
    ;
}

- (NSUInteger)hash
{
    [self readAll:YES];
    NSUInteger value = 0;

    return value
    ^
    (self->_flags.has_transitLineMuid ? PBHashInt((NSUInteger)self->_transitLineMuid) : 0)
    ^
    [self->_transitLineName hash]
    ^
    [self->_transitSystemName hash]
    ;
}

- (void)mergeFrom:(GEORPTransitLineTileInfo *)other
{
    [other readAll:NO];
    if (other->_flags.has_transitLineMuid)
    {
        self->_transitLineMuid = other->_transitLineMuid;
        self->_flags.has_transitLineMuid = 1;
    }
    if (other->_transitLineName)
    {
        [self setTransitLineName:other->_transitLineName];
    }
    if (other->_transitSystemName)
    {
        [self setTransitSystemName:other->_transitSystemName];
    }
}


@end


