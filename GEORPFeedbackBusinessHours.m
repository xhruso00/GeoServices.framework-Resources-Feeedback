// This file was automatically generated by gyb
// DO NOT EDIT!
// Compiled from community_feedback.proto

#import "GEORPFeedbackBusinessHours.h"
#import <ProtocolBuffer/PBConstants.h>
#import <ProtocolBuffer/PBHashUtil.h>
#import <ProtocolBuffer/PBDataReader.h>
#import <os/lock_private.h>

#import <GeoServices/GEOPDHours.h>

#if !__has_feature(objc_arc)
# error This generated file depends on ARC but it is not enabled
#endif

@implementation GEORPFeedbackBusinessHours

- (instancetype)init
{
    self = [super init];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (instancetype)initWithData:(NSData *)data
{
    self = [super initWithData:data];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (void)_readWeeklyHours {
    if (_flags.read_weeklyHours) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_weeklyHours) {
            static const uint32_t tags[] = {1, 0};
            GEORPFeedbackBusinessHoursReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasWeeklyHours
{
    [self _readWeeklyHours];
    return _weeklyHours != nil;
}
- (GEOPDHours *)weeklyHours
{
    [self _readWeeklyHours];
    return _weeklyHours;
}
- (void)setWeeklyHours:(GEOPDHours *)v
{
    _flags.read_weeklyHours = 1;
    _flags.wrote_weeklyHours = 1;
    _weeklyHours = v;
}

- (uint64_t)start
{
    return _start;
}
- (void)setStart:(uint64_t)v
{
    _flags.wrote_start = 1;
    _flags.has_start = 1;
    _start = v;
}
- (void)setHasStart:(BOOL)f
{
    _flags.wrote_start = 1;
    _flags.has_start = f ? 1 : 0;
}
- (BOOL)hasStart
{
    return (BOOL)_flags.has_start;
}

- (uint64_t)end
{
    return _end;
}
- (void)setEnd:(uint64_t)v
{
    _flags.wrote_end = 1;
    _flags.has_end = 1;
    _end = v;
}
- (void)setHasEnd:(BOOL)f
{
    _flags.wrote_end = 1;
    _flags.has_end = f ? 1 : 0;
}
- (BOOL)hasEnd
{
    return (BOOL)_flags.has_end;
}

- (void)_readComments {
    if (_flags.read_comments) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_comments) {
            static const uint32_t tags[] = {4, 0};
            GEORPFeedbackBusinessHoursReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasComments
{
    [self _readComments];
    return _comments != nil;
}
- (NSString *)comments
{
    [self _readComments];
    return _comments;
}
- (void)setComments:(NSString *)v
{
    _flags.read_comments = 1;
    _flags.wrote_comments = 1;
    _comments = v;
}

- (GEORPFeedbackBusinessHours_FeedbackHoursType)hoursType
{
    return _flags.has_hoursType ? _hoursType : GEORPFeedbackBusinessHours_FeedbackHoursType_FEEDBACK_HOURS_UNKNOWN;
}
- (void)setHoursType:(GEORPFeedbackBusinessHours_FeedbackHoursType)v
{
    _flags.wrote_hoursType = 1;
    _flags.has_hoursType = 1;
    _hoursType = v;
}
- (void)setHasHoursType:(BOOL)f
{
    _flags.wrote_hoursType = 1;
    _flags.has_hoursType = f ? 1 : 0;
}
- (BOOL)hasHoursType
{
    return (BOOL)_flags.has_hoursType;
}
- (NSString *)hoursTypeAsString:(GEORPFeedbackBusinessHours_FeedbackHoursType)value
{
    return GEORPFeedbackBusinessHours_FeedbackHoursTypeAsString(value);
}
- (GEORPFeedbackBusinessHours_FeedbackHoursType)StringAsHoursType:(NSString *)str
{
    return StringAsGEORPFeedbackBusinessHours_FeedbackHoursType(str);
}


- (NSString *)description
{
    return [NSString stringWithFormat:@"%@ %@", [super description], [self dictionaryRepresentation]];
}

- (NSDictionary *)dictionaryRepresentation
{
    [self readAll:YES];
    NSMutableDictionary *dict = [NSMutableDictionary dictionary];
    {
        GEOPDHours * value = self.weeklyHours;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"weekly_hours"];
        }
    }

    if (self->_flags.has_start)
    {
        [dict setObject:@(self->_start) forKey:@"start"];
    }

    if (self->_flags.has_end)
    {
        [dict setObject:@(self->_end) forKey:@"end"];
    }

    {
        NSString * value = self.comments;
        if (value) {
            [dict setObject:value forKey:@"comments"];
        }
    }

    if (self->_flags.has_hoursType)
    {
        [dict setObject:GEORPFeedbackBusinessHours_FeedbackHoursTypeAsString(self->_hoursType) forKey:@"hours_type"];
    }


    if (_unknownFields)
        [dict setObject:[_unknownFields dictionaryRepresentation] forKey:@"Unknown Fields"];

    return dict;
}


#define TAG_PARSE_ALL_RECURSIVE     (UINT32_MAX)
#define TAG_PARSE_ALL_NON_RECURSIVE (UINT32_MAX-1)
#define TAG_PARSE_INITIAL           (UINT32_MAX-2)
#define TAG_UNKNOWN_FIELDS          (UINT32_MAX-3)

- (void)readAll:(BOOL)recursively {
    os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (reader == nil) return;
    @synchronized(reader) {
        static const uint32_t recursiveTag[] = {TAG_PARSE_ALL_RECURSIVE, 0};
        static const uint32_t nonRecursiveTag[] = {TAG_PARSE_ALL_NON_RECURSIVE, 0};
        GEORPFeedbackBusinessHoursReadSpecified(self, reader, recursively ? recursiveTag : nonRecursiveTag);
    }
}
BOOL GEORPFeedbackBusinessHoursReadAllFrom(__unsafe_unretained GEORPFeedbackBusinessHours *self, __unsafe_unretained PBDataReader *reader, BOOL recursively) {
    if (self->_readerMarkLength != 0) {
        [NSException raise:NSInvalidArgumentException format:@"GEORPFeedbackBusinessHoursReadAllFrom can only be called once per object"];
        return NO;
    }

    if (reader.position > UINT32_MAX || reader.length > UINT32_MAX) {
        os_log_fault(OS_LOG_DEFAULT, "Cannot decode data with position/length > UINT32_MAX");
        return NO;
    }

    static const uint32_t recursiveTag[] = {TAG_PARSE_INITIAL, TAG_PARSE_ALL_RECURSIVE, 0};
    static const uint32_t initialTag[] = {TAG_PARSE_INITIAL, 0};

    @synchronized(reader) {
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = reader;
        os_unfair_lock_unlock(&(self->_readerLock));

        self->_readerMarkPos = (uint32_t)reader.position;
        self->_readerMarkLength = (uint32_t)reader.length;
        BOOL result = GEORPFeedbackBusinessHoursReadSpecified(self, reader, recursively ? recursiveTag : initialTag);
        return result;
    }
}

BOOL GEORPFeedbackBusinessHoursReadFrom(__unsafe_unretained GEORPFeedbackBusinessHours *self, __unsafe_unretained PBDataReader *reader) {
    @synchronized(reader) {
        return GEORPFeedbackBusinessHoursReadAllFrom(self, reader, NO);
    }
}

BOOL GEORPFeedbackBusinessHoursReadSpecified(__unsafe_unretained GEORPFeedbackBusinessHours *self, __unsafe_unretained PBDataReader *reader, const uint32_t *tags) {
    if (reader == nil) {
        return NO;
    }
    reader->_length = (NSUInteger)self->_readerMarkLength;
    [reader seekToOffset:(NSUInteger)self->_readerMarkPos];
    uint32_t readLazyCount = 0
        + self->_flags.read_weeklyHours
        + self->_flags.read_comments
        + self->_flags.read_unknownFields
        ;
    #define requiredLazyFieldCount 1 // objc_msg_parse_all_threshold = 0.8

    BOOL readAll = NO;
    __attribute__((unused)) BOOL recursively = NO;
    BOOL shouldParseUnknown = NO;
    BOOL isInitial = NO;
    size_t tagsRemaining = 0;
    for (; tags[tagsRemaining] != 0; ++tagsRemaining) {
        switch (tags[tagsRemaining]) {
            case TAG_PARSE_ALL_RECURSIVE:
                recursively = YES;
                readAll = YES;
                shouldParseUnknown = YES;
                break;
            case TAG_PARSE_ALL_NON_RECURSIVE:
                readAll = YES;
                shouldParseUnknown = YES;
                break;
            case TAG_PARSE_INITIAL:
                isInitial = YES;
                break;
            case TAG_UNKNOWN_FIELDS:
                shouldParseUnknown = YES;
                ++readLazyCount;
                break;
            default:
                ++readLazyCount;
                break;
            }
    }
    if (!readAll && readLazyCount >= requiredLazyFieldCount) {
        readAll = YES;
        shouldParseUnknown = YES;
    }
    const BOOL stopWhenNoMoreTagsRemaining = !(readAll || shouldParseUnknown);
    if (readAll || isInitial) tags = NULL;

    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }
        
        BOOL shouldParseTag = readAll;
        for (size_t i = 0; tags != NULL && !shouldParseTag && tags[i] != 0; ++i) {
            shouldParseTag = (tags[i] == tag);
        }

        switch (tag) {

            case 1 /* weeklyHours */:
            {
                if (!shouldParseTag || self->_flags.read_weeklyHours) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_weeklyHours) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEOPDHours *new_weeklyHours = [[GEOPDHours alloc] init];
                self->_weeklyHours = new_weeklyHours;
                PBDataReaderMark mark_weeklyHours;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_weeklyHours);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEOPDHoursReadAllFrom(new_weeklyHours, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_weeklyHours);

                --tagsRemaining;
            }
            break;
            case 2 /* start */:
            {
                if (!isInitial) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                self->_flags.has_start = YES;
                self->_start = PBReaderReadUint64(reader);

            }
            break;
            case 3 /* end */:
            {
                if (!isInitial) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                self->_flags.has_end = YES;
                self->_end = PBReaderReadUint64(reader);

            }
            break;
            case 4 /* comments */:
            {
                if (!shouldParseTag || self->_flags.read_comments) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_comments) {
                        --tagsRemaining;
                    }
                    break;
                }
                NSString * new_comments = PBReaderReadString(reader);
                self->_comments = new_comments;

                --tagsRemaining;
            }
            break;
            case 5 /* hoursType */:
            {
                if (!isInitial) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                self->_flags.has_hoursType = YES;
                self->_hoursType = PBReaderReadInt32(reader);

            }
            break;
            default:
                if (!shouldParseUnknown || self->_flags.read_unknownFields) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                if (!self->_unknownFields)
                    self->_unknownFields = [[PBUnknownFields alloc] init];
                if (!PBUnknownFieldAdd(self->_unknownFields, tag, aType, reader))
                    return NO;
                break;
        }
        if (stopWhenNoMoreTagsRemaining && tagsRemaining == 0) {
            break;
        }
    }

    if (shouldParseUnknown) {
        self->_flags.read_unknownFields = YES;
    }

    if (readAll) {
        if (recursively) {
            [self->_weeklyHours readAll:YES];
        }
        self->_flags.read_weeklyHours = YES;
        self->_flags.read_comments = YES;
    } else {
        for (size_t i = 0; tags != NULL && tags[i] != 0; ++i) {
            switch (tags[i]) {
                case 1 /* weeklyHours */:
                    self->_flags.read_weeklyHours = YES;
                    break;
                case 4 /* comments */:
                    self->_flags.read_comments = YES;
                    break;
            }
        }
    }

    #undef requiredLazyFieldCount
    if (readAll) {
        // reader is __unsafe_unretained
        const BOOL hadError = PBReaderHasError(reader);
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = nil;
        os_unfair_lock_unlock(&(self->_readerLock));
        return !hadError;
    }
    return !PBReaderHasError(reader);
}

+ (BOOL)isValid:(__unsafe_unretained PBDataReader *)reader
{
    return GEORPFeedbackBusinessHoursIsValid(reader);
}

BOOL GEORPFeedbackBusinessHoursIsValid(__unsafe_unretained PBDataReader *reader) {
    PBDataReaderMark resetMark;
    resetMark.pos = reader.position;
    resetMark.length = reader.length;
    #define returnValue(X) do { \
        const BOOL v = X; \
        PBReaderRecallMark(reader, &resetMark); \
        return v; \
    } while(0)
    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }

        switch (tag) {

            case 1 /* weeklyHours */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_weeklyHours;
                    if (!PBReaderPlaceMark(reader, &mark_weeklyHours))
                        returnValue(NO);
                    if (!GEOPDHoursIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_weeklyHours);
                }
                break;
            case 2 /* start */:
                if (TYPE_VARINT != aType)
                    returnValue(NO);
                PBReaderReadUint64(reader);
                break;
            case 3 /* end */:
                if (TYPE_VARINT != aType)
                    returnValue(NO);
                PBReaderReadUint64(reader);
                break;
            case 4 /* comments */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
                break;
            case 5 /* hoursType */:
                if (TYPE_VARINT != aType)
                    returnValue(NO);
                PBReaderReadInt32(reader);
                break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
        }
    }
    returnValue(!PBReaderHasError(reader));
    #undef returnValue
}

- (BOOL)readFrom:(PBDataReader *)reader
{
    return GEORPFeedbackBusinessHoursReadAllFrom(self, reader, NO);
}

- (void)writeTo:(PBDataWriter *)writer
{
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            if (!GEORPFeedbackBusinessHoursIsDirty(self)) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                uint32_t length = (_readerMarkLength - _readerMarkPos);
                NSData *data = [reader readBytes:length];
                [writer writeData:data];
                return;
            }
        }
    }

    [self readAll:NO];

    /* weeklyHours */
    {
        if (self->_weeklyHours != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_weeklyHours, 1);
        }
    }

    /* start */
    {
        if (self->_flags.has_start)
        {
            PBDataWriterWriteUint64Field(writer, self->_start, 2);
        }
    }

    /* end */
    {
        if (self->_flags.has_end)
        {
            PBDataWriterWriteUint64Field(writer, self->_end, 3);
        }
    }

    /* comments */
    {
        if (self->_comments)
        {
            PBDataWriterWriteStringField(writer, self->_comments, 4);
        }
    }

    /* hoursType */
    {
        if (self->_flags.has_hoursType)
        {
            PBDataWriterWriteInt32Field(writer, self->_hoursType, 5);
        }
    }
    /* Unknown Fields */
    {
        [_unknownFields writeTo:writer];
    }
}

BOOL GEORPFeedbackBusinessHoursIsDirty(__unsafe_unretained GEORPFeedbackBusinessHours *self)
{
    if (self == nil) {
        return NO;
    }
    
    BOOL dirty = NO
        || self->_flags.wrote_weeklyHours
        || (self->_flags.read_weeklyHours && GEOPDHoursIsDirty(self->_weeklyHours))
        || self->_flags.wrote_start
        || self->_flags.wrote_end
        || self->_flags.wrote_comments
        || self->_flags.read_comments
        || self->_flags.wrote_hoursType
        || self->_flags.wrote_unknownFields
    ;


    return dirty;
}


- (void)copyTo:(GEORPFeedbackBusinessHours *)other
{
    [self readAll:NO];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    other->_reader = _reader;
    other->_readerMarkPos = _readerMarkPos;
    other->_readerMarkLength = _readerMarkLength;
    os_unfair_lock_unlock(&_readerLock);
    if (_weeklyHours)
    {
        other.weeklyHours = _weeklyHours;
    }
    if (self->_flags.has_start)
    {
        other->_start = _start;
        other->_flags.has_start = YES;
    }
    if (self->_flags.has_end)
    {
        other->_end = _end;
        other->_flags.has_end = YES;
    }
    if (_comments)
    {
        other.comments = _comments;
    }
    if (self->_flags.has_hoursType)
    {
        other->_hoursType = _hoursType;
        other->_flags.has_hoursType = YES;
    }
}

- (id)copyWithZone:(NSZone *)zone
{
    GEORPFeedbackBusinessHours *copy = [[[self class] allocWithZone:zone] init];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            BOOL wroteAny = NO
                || _flags.wrote_weeklyHours
                || _flags.wrote_start
                || _flags.wrote_end
                || _flags.wrote_comments
                || _flags.wrote_hoursType
                || _flags.wrote_unknownFields
            ;
            if (!wroteAny) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                GEORPFeedbackBusinessHoursReadAllFrom(copy, reader, NO);
                return copy;
            }
            [self readAll:NO];
        }
    }
    copy->_weeklyHours = [_weeklyHours copyWithZone:zone];
    if (self->_flags.has_start)
    {
        copy->_start = _start;
        copy->_flags.has_start = YES;
    }
    if (self->_flags.has_end)
    {
        copy->_end = _end;
        copy->_flags.has_end = YES;
    }
    copy->_comments = [_comments copyWithZone:zone];
    if (self->_flags.has_hoursType)
    {
        copy->_hoursType = _hoursType;
        copy->_flags.has_hoursType = YES;
    }
    copy->_unknownFields = _unknownFields;
    return copy;
}


- (BOOL)isEqual:(id)object
{
    GEORPFeedbackBusinessHours *other = (GEORPFeedbackBusinessHours *)object;
    if (![other isMemberOfClass:[self class]]) {
        return NO;
    }
    [self readAll:YES];
    [other readAll:YES];


    return YES
    &&
    ((!self->_weeklyHours && !other->_weeklyHours) || [self->_weeklyHours isEqual:other->_weeklyHours])
    &&
    ((self->_flags.has_start && other->_flags.has_start && self->_start == other->_start) || (!self->_flags.has_start && !other->_flags.has_start))
    &&
    ((self->_flags.has_end && other->_flags.has_end && self->_end == other->_end) || (!self->_flags.has_end && !other->_flags.has_end))
    &&
    ((!self->_comments && !other->_comments) || [self->_comments isEqual:other->_comments])
    &&
    ((self->_flags.has_hoursType && other->_flags.has_hoursType && self->_hoursType == other->_hoursType) || (!self->_flags.has_hoursType && !other->_flags.has_hoursType))
    ;
}

- (NSUInteger)hash
{
    [self readAll:YES];
    NSUInteger value = 0;

    return value
    ^
    [self->_weeklyHours hash]
    ^
    (self->_flags.has_start ? PBHashInt((NSUInteger)self->_start) : 0)
    ^
    (self->_flags.has_end ? PBHashInt((NSUInteger)self->_end) : 0)
    ^
    [self->_comments hash]
    ^
    (self->_flags.has_hoursType ? PBHashInt((NSUInteger)self->_hoursType) : 0)
    ;
}

- (void)mergeFrom:(GEORPFeedbackBusinessHours *)other
{
    [other readAll:NO];
    if (self->_weeklyHours && other->_weeklyHours)
    {
        [self->_weeklyHours mergeFrom:other->_weeklyHours];
    }
    else if (!self->_weeklyHours && other->_weeklyHours)
    {
        [self setWeeklyHours:other->_weeklyHours];
    }
    if (other->_flags.has_start)
    {
        self->_start = other->_start;
        self->_flags.has_start = 1;
    }
    if (other->_flags.has_end)
    {
        self->_end = other->_end;
        self->_flags.has_end = 1;
    }
    if (other->_comments)
    {
        [self setComments:other->_comments];
    }
    if (other->_flags.has_hoursType)
    {
        self->_hoursType = other->_hoursType;
        self->_flags.has_hoursType = 1;
    }
}

- (PBUnknownFields *)unknownFields
{
    if (_flags.read_unknownFields) return _unknownFields;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return _unknownFields;
    @synchronized(reader) {
        if (!_flags.read_unknownFields) {
            static const uint32_t tags[] = {TAG_UNKNOWN_FIELDS, 0};
            GEORPFeedbackBusinessHoursReadSpecified(self, reader, tags);
        }
    }
    return _unknownFields;
}
- (void)clearUnknownFields:(BOOL)recursively
{
    _flags.read_unknownFields = 1;
    _flags.wrote_unknownFields = 1;
    _unknownFields = nil;
    if (!recursively) {
        return;
    }
    [self readAll:NO];
    [_weeklyHours clearUnknownFields:YES];
}

@end


