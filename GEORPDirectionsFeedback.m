// This file was automatically generated by gyb
// DO NOT EDIT!
// Compiled from community_feedback.proto

#import "GEORPDirectionsFeedback.h"
#import <ProtocolBuffer/PBConstants.h>
#import <ProtocolBuffer/PBHashUtil.h>
#import <ProtocolBuffer/PBDataReader.h>
#import <os/lock_private.h>

#import <GeoServices/GEORPDirectionsCorrections.h>
#import <GeoServices/GEORPDirectionsFeedbackContext.h>

#if !__has_feature(objc_arc)
# error This generated file depends on ARC but it is not enabled
#endif

@implementation GEORPDirectionsFeedback

- (instancetype)init
{
    self = [super init];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (instancetype)initWithData:(NSData *)data
{
    self = [super initWithData:data];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (void)_readDirectionsContext {
    if (_flags.read_directionsContext) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_directionsContext) {
            static const uint32_t tags[] = {1, 0};
            GEORPDirectionsFeedbackReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasDirectionsContext
{
    [self _readDirectionsContext];
    return _directionsContext != nil;
}
- (GEORPDirectionsFeedbackContext *)directionsContext
{
    [self _readDirectionsContext];
    return _directionsContext;
}
- (void)setDirectionsContext:(GEORPDirectionsFeedbackContext *)v
{
    _flags.read_directionsContext = 1;
    _flags.wrote_directionsContext = 1;
    _directionsContext = v;
}

- (void)_readDirectionsCorrections {
    if (_flags.read_directionsCorrections) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_directionsCorrections) {
            static const uint32_t tags[] = {2, 0};
            GEORPDirectionsFeedbackReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasDirectionsCorrections
{
    [self _readDirectionsCorrections];
    return _directionsCorrections != nil;
}
- (GEORPDirectionsCorrections *)directionsCorrections
{
    [self _readDirectionsCorrections];
    return _directionsCorrections;
}
- (void)setDirectionsCorrections:(GEORPDirectionsCorrections *)v
{
    _flags.read_directionsCorrections = 1;
    _flags.wrote_directionsCorrections = 1;
    _directionsCorrections = v;
}

- (GEORPDirectionsFeedback_DirectionsCorrectionType)correctionType
{
    return _flags.has_correctionType ? _correctionType : GEORPDirectionsFeedback_DirectionsCorrectionType_DIRECTIONS_CORRECTION_TYPE_UNKNOWN;
}
- (void)setCorrectionType:(GEORPDirectionsFeedback_DirectionsCorrectionType)v
{
    _flags.wrote_correctionType = 1;
    _flags.has_correctionType = 1;
    _correctionType = v;
}
- (void)setHasCorrectionType:(BOOL)f
{
    _flags.wrote_correctionType = 1;
    _flags.has_correctionType = f ? 1 : 0;
}
- (BOOL)hasCorrectionType
{
    return (BOOL)_flags.has_correctionType;
}
- (NSString *)correctionTypeAsString:(GEORPDirectionsFeedback_DirectionsCorrectionType)value
{
    return GEORPDirectionsFeedback_DirectionsCorrectionTypeAsString(value);
}
- (GEORPDirectionsFeedback_DirectionsCorrectionType)StringAsCorrectionType:(NSString *)str
{
    return StringAsGEORPDirectionsFeedback_DirectionsCorrectionType(str);
}


- (NSString *)description
{
    return [NSString stringWithFormat:@"%@ %@", [super description], [self dictionaryRepresentation]];
}

- (NSDictionary *)dictionaryRepresentation
{
    [self readAll:YES];
    NSMutableDictionary *dict = [NSMutableDictionary dictionary];
    {
        GEORPDirectionsFeedbackContext * value = self.directionsContext;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"directions_context"];
        }
    }

    {
        GEORPDirectionsCorrections * value = self.directionsCorrections;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"directions_corrections"];
        }
    }

    if (self->_flags.has_correctionType)
    {
        [dict setObject:GEORPDirectionsFeedback_DirectionsCorrectionTypeAsString(self->_correctionType) forKey:@"correction_type"];
    }


    if (_unknownFields)
        [dict setObject:[_unknownFields dictionaryRepresentation] forKey:@"Unknown Fields"];

    return dict;
}


#define TAG_PARSE_ALL_RECURSIVE     (UINT32_MAX)
#define TAG_PARSE_ALL_NON_RECURSIVE (UINT32_MAX-1)
#define TAG_PARSE_INITIAL           (UINT32_MAX-2)
#define TAG_UNKNOWN_FIELDS          (UINT32_MAX-3)

- (void)readAll:(BOOL)recursively {
    os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (reader == nil) return;
    @synchronized(reader) {
        static const uint32_t recursiveTag[] = {TAG_PARSE_ALL_RECURSIVE, 0};
        static const uint32_t nonRecursiveTag[] = {TAG_PARSE_ALL_NON_RECURSIVE, 0};
        GEORPDirectionsFeedbackReadSpecified(self, reader, recursively ? recursiveTag : nonRecursiveTag);
    }
}
BOOL GEORPDirectionsFeedbackReadAllFrom(__unsafe_unretained GEORPDirectionsFeedback *self, __unsafe_unretained PBDataReader *reader, BOOL recursively) {
    if (self->_readerMarkLength != 0) {
        [NSException raise:NSInvalidArgumentException format:@"GEORPDirectionsFeedbackReadAllFrom can only be called once per object"];
        return NO;
    }

    if (reader.position > UINT32_MAX || reader.length > UINT32_MAX) {
        os_log_fault(OS_LOG_DEFAULT, "Cannot decode data with position/length > UINT32_MAX");
        return NO;
    }

    static const uint32_t recursiveTag[] = {TAG_PARSE_INITIAL, TAG_PARSE_ALL_RECURSIVE, 0};
    static const uint32_t initialTag[] = {TAG_PARSE_INITIAL, 0};

    @synchronized(reader) {
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = reader;
        os_unfair_lock_unlock(&(self->_readerLock));

        self->_readerMarkPos = (uint32_t)reader.position;
        self->_readerMarkLength = (uint32_t)reader.length;
        BOOL result = GEORPDirectionsFeedbackReadSpecified(self, reader, recursively ? recursiveTag : initialTag);
        return result;
    }
}

BOOL GEORPDirectionsFeedbackReadFrom(__unsafe_unretained GEORPDirectionsFeedback *self, __unsafe_unretained PBDataReader *reader) {
    @synchronized(reader) {
        return GEORPDirectionsFeedbackReadAllFrom(self, reader, NO);
    }
}

BOOL GEORPDirectionsFeedbackReadSpecified(__unsafe_unretained GEORPDirectionsFeedback *self, __unsafe_unretained PBDataReader *reader, const uint32_t *tags) {
    if (reader == nil) {
        return NO;
    }
    reader->_length = (NSUInteger)self->_readerMarkLength;
    [reader seekToOffset:(NSUInteger)self->_readerMarkPos];
    uint32_t readLazyCount = 0
        + self->_flags.read_directionsContext
        + self->_flags.read_directionsCorrections
        + self->_flags.read_unknownFields
        ;
    #define requiredLazyFieldCount 1 // objc_msg_parse_all_threshold = 0.8

    BOOL readAll = NO;
    __attribute__((unused)) BOOL recursively = NO;
    BOOL shouldParseUnknown = NO;
    BOOL isInitial = NO;
    size_t tagsRemaining = 0;
    for (; tags[tagsRemaining] != 0; ++tagsRemaining) {
        switch (tags[tagsRemaining]) {
            case TAG_PARSE_ALL_RECURSIVE:
                recursively = YES;
                readAll = YES;
                shouldParseUnknown = YES;
                break;
            case TAG_PARSE_ALL_NON_RECURSIVE:
                readAll = YES;
                shouldParseUnknown = YES;
                break;
            case TAG_PARSE_INITIAL:
                isInitial = YES;
                break;
            case TAG_UNKNOWN_FIELDS:
                shouldParseUnknown = YES;
                ++readLazyCount;
                break;
            default:
                ++readLazyCount;
                break;
            }
    }
    if (!readAll && readLazyCount >= requiredLazyFieldCount) {
        readAll = YES;
        shouldParseUnknown = YES;
    }
    const BOOL stopWhenNoMoreTagsRemaining = !(readAll || shouldParseUnknown);
    if (readAll || isInitial) tags = NULL;

    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }
        
        BOOL shouldParseTag = readAll;
        for (size_t i = 0; tags != NULL && !shouldParseTag && tags[i] != 0; ++i) {
            shouldParseTag = (tags[i] == tag);
        }

        switch (tag) {

            case 1 /* directionsContext */:
            {
                if (!shouldParseTag || self->_flags.read_directionsContext) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_directionsContext) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEORPDirectionsFeedbackContext *new_directionsContext = [[GEORPDirectionsFeedbackContext alloc] init];
                self->_directionsContext = new_directionsContext;
                PBDataReaderMark mark_directionsContext;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_directionsContext);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEORPDirectionsFeedbackContextReadAllFrom(new_directionsContext, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_directionsContext);

                --tagsRemaining;
            }
            break;
            case 2 /* directionsCorrections */:
            {
                if (!shouldParseTag || self->_flags.read_directionsCorrections) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_directionsCorrections) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEORPDirectionsCorrections *new_directionsCorrections = [[GEORPDirectionsCorrections alloc] init];
                self->_directionsCorrections = new_directionsCorrections;
                PBDataReaderMark mark_directionsCorrections;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_directionsCorrections);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEORPDirectionsCorrectionsReadAllFrom(new_directionsCorrections, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_directionsCorrections);

                --tagsRemaining;
            }
            break;
            case 3 /* correctionType */:
            {
                if (!isInitial) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                self->_flags.has_correctionType = YES;
                self->_correctionType = PBReaderReadInt32(reader);

            }
            break;
            default:
                if (!shouldParseUnknown || self->_flags.read_unknownFields) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                if (!self->_unknownFields)
                    self->_unknownFields = [[PBUnknownFields alloc] init];
                if (!PBUnknownFieldAdd(self->_unknownFields, tag, aType, reader))
                    return NO;
                break;
        }
        if (stopWhenNoMoreTagsRemaining && tagsRemaining == 0) {
            break;
        }
    }

    if (shouldParseUnknown) {
        self->_flags.read_unknownFields = YES;
    }

    if (readAll) {
        if (recursively) {
            [self->_directionsContext readAll:YES];
            [self->_directionsCorrections readAll:YES];
        }
        self->_flags.read_directionsContext = YES;
        self->_flags.read_directionsCorrections = YES;
    } else {
        for (size_t i = 0; tags != NULL && tags[i] != 0; ++i) {
            switch (tags[i]) {
                case 1 /* directionsContext */:
                    self->_flags.read_directionsContext = YES;
                    break;
                case 2 /* directionsCorrections */:
                    self->_flags.read_directionsCorrections = YES;
                    break;
            }
        }
    }

    #undef requiredLazyFieldCount
    if (readAll) {
        // reader is __unsafe_unretained
        const BOOL hadError = PBReaderHasError(reader);
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = nil;
        os_unfair_lock_unlock(&(self->_readerLock));
        return !hadError;
    }
    return !PBReaderHasError(reader);
}

+ (BOOL)isValid:(__unsafe_unretained PBDataReader *)reader
{
    return GEORPDirectionsFeedbackIsValid(reader);
}

BOOL GEORPDirectionsFeedbackIsValid(__unsafe_unretained PBDataReader *reader) {
    PBDataReaderMark resetMark;
    resetMark.pos = reader.position;
    resetMark.length = reader.length;
    #define returnValue(X) do { \
        const BOOL v = X; \
        PBReaderRecallMark(reader, &resetMark); \
        return v; \
    } while(0)
    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }

        switch (tag) {

            case 1 /* directionsContext */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_directionsContext;
                    if (!PBReaderPlaceMark(reader, &mark_directionsContext))
                        returnValue(NO);
                    if (!GEORPDirectionsFeedbackContextIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_directionsContext);
                }
                break;
            case 2 /* directionsCorrections */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_directionsCorrections;
                    if (!PBReaderPlaceMark(reader, &mark_directionsCorrections))
                        returnValue(NO);
                    if (!GEORPDirectionsCorrectionsIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_directionsCorrections);
                }
                break;
            case 3 /* correctionType */:
                if (TYPE_VARINT != aType)
                    returnValue(NO);
                PBReaderReadInt32(reader);
                break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
        }
    }
    returnValue(!PBReaderHasError(reader));
    #undef returnValue
}

- (BOOL)readFrom:(PBDataReader *)reader
{
    return GEORPDirectionsFeedbackReadAllFrom(self, reader, NO);
}

- (void)writeTo:(PBDataWriter *)writer
{
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            if (!GEORPDirectionsFeedbackIsDirty(self)) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                uint32_t length = (_readerMarkLength - _readerMarkPos);
                NSData *data = [reader readBytes:length];
                [writer writeData:data];
                return;
            }
        }
    }

    [self readAll:NO];

    /* directionsContext */
    {
        if (self->_directionsContext != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_directionsContext, 1);
        }
    }

    /* directionsCorrections */
    {
        if (self->_directionsCorrections != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_directionsCorrections, 2);
        }
    }

    /* correctionType */
    {
        if (self->_flags.has_correctionType)
        {
            PBDataWriterWriteInt32Field(writer, self->_correctionType, 3);
        }
    }
    /* Unknown Fields */
    {
        [_unknownFields writeTo:writer];
    }
}

BOOL GEORPDirectionsFeedbackIsDirty(__unsafe_unretained GEORPDirectionsFeedback *self)
{
    if (self == nil) {
        return NO;
    }
    
    BOOL dirty = NO
        || self->_flags.wrote_directionsContext
        || (self->_flags.read_directionsContext && GEORPDirectionsFeedbackContextIsDirty(self->_directionsContext))
        || self->_flags.wrote_directionsCorrections
        || (self->_flags.read_directionsCorrections && GEORPDirectionsCorrectionsIsDirty(self->_directionsCorrections))
        || self->_flags.wrote_correctionType
        || self->_flags.wrote_unknownFields
    ;


    return dirty;
}


- (void)copyTo:(GEORPDirectionsFeedback *)other
{
    [self readAll:NO];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    other->_reader = _reader;
    other->_readerMarkPos = _readerMarkPos;
    other->_readerMarkLength = _readerMarkLength;
    os_unfair_lock_unlock(&_readerLock);
    if (_directionsContext)
    {
        other.directionsContext = _directionsContext;
    }
    if (_directionsCorrections)
    {
        other.directionsCorrections = _directionsCorrections;
    }
    if (self->_flags.has_correctionType)
    {
        other->_correctionType = _correctionType;
        other->_flags.has_correctionType = YES;
    }
}

- (id)copyWithZone:(NSZone *)zone
{
    GEORPDirectionsFeedback *copy = [[[self class] allocWithZone:zone] init];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            BOOL wroteAny = NO
                || _flags.wrote_directionsContext
                || _flags.wrote_directionsCorrections
                || _flags.wrote_correctionType
                || _flags.wrote_unknownFields
            ;
            if (!wroteAny) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                GEORPDirectionsFeedbackReadAllFrom(copy, reader, NO);
                return copy;
            }
            [self readAll:NO];
        }
    }
    copy->_directionsContext = [_directionsContext copyWithZone:zone];
    copy->_directionsCorrections = [_directionsCorrections copyWithZone:zone];
    if (self->_flags.has_correctionType)
    {
        copy->_correctionType = _correctionType;
        copy->_flags.has_correctionType = YES;
    }
    copy->_unknownFields = _unknownFields;
    return copy;
}


- (BOOL)isEqual:(id)object
{
    GEORPDirectionsFeedback *other = (GEORPDirectionsFeedback *)object;
    if (![other isMemberOfClass:[self class]]) {
        return NO;
    }
    [self readAll:YES];
    [other readAll:YES];


    return YES
    &&
    ((!self->_directionsContext && !other->_directionsContext) || [self->_directionsContext isEqual:other->_directionsContext])
    &&
    ((!self->_directionsCorrections && !other->_directionsCorrections) || [self->_directionsCorrections isEqual:other->_directionsCorrections])
    &&
    ((self->_flags.has_correctionType && other->_flags.has_correctionType && self->_correctionType == other->_correctionType) || (!self->_flags.has_correctionType && !other->_flags.has_correctionType))
    ;
}

- (NSUInteger)hash
{
    [self readAll:YES];
    NSUInteger value = 0;

    return value
    ^
    [self->_directionsContext hash]
    ^
    [self->_directionsCorrections hash]
    ^
    (self->_flags.has_correctionType ? PBHashInt((NSUInteger)self->_correctionType) : 0)
    ;
}

- (void)mergeFrom:(GEORPDirectionsFeedback *)other
{
    [other readAll:NO];
    if (self->_directionsContext && other->_directionsContext)
    {
        [self->_directionsContext mergeFrom:other->_directionsContext];
    }
    else if (!self->_directionsContext && other->_directionsContext)
    {
        [self setDirectionsContext:other->_directionsContext];
    }
    if (self->_directionsCorrections && other->_directionsCorrections)
    {
        [self->_directionsCorrections mergeFrom:other->_directionsCorrections];
    }
    else if (!self->_directionsCorrections && other->_directionsCorrections)
    {
        [self setDirectionsCorrections:other->_directionsCorrections];
    }
    if (other->_flags.has_correctionType)
    {
        self->_correctionType = other->_correctionType;
        self->_flags.has_correctionType = 1;
    }
}

- (PBUnknownFields *)unknownFields
{
    if (_flags.read_unknownFields) return _unknownFields;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return _unknownFields;
    @synchronized(reader) {
        if (!_flags.read_unknownFields) {
            static const uint32_t tags[] = {TAG_UNKNOWN_FIELDS, 0};
            GEORPDirectionsFeedbackReadSpecified(self, reader, tags);
        }
    }
    return _unknownFields;
}
- (void)clearUnknownFields:(BOOL)recursively
{
    _flags.read_unknownFields = 1;
    _flags.wrote_unknownFields = 1;
    _unknownFields = nil;
    if (!recursively) {
        return;
    }
    [self readAll:NO];
    [_directionsContext clearUnknownFields:YES];
    [_directionsCorrections clearUnknownFields:YES];
}

@end


