// This file was automatically generated by gyb
// DO NOT EDIT!
// Compiled from reportaproblem.proto

#import "GEORPCorrectedCoordinate.h"
#import <ProtocolBuffer/PBConstants.h>
#import <ProtocolBuffer/PBHashUtil.h>
#import <ProtocolBuffer/PBDataReader.h>
#import <os/lock_private.h>

#import <GeoServices/GEOLatLng.h>

#if !__has_feature(objc_arc)
# error This generated file depends on ARC but it is not enabled
#endif

@implementation GEORPCorrectedCoordinate

- (instancetype)init
{
    self = [super init];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (instancetype)initWithData:(NSData *)data
{
    self = [super initWithData:data];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (void)_readOriginalCoordinate {
    if (_flags.read_originalCoordinate) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_originalCoordinate) {
            static const uint32_t tags[] = {1, 0};
            GEORPCorrectedCoordinateReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasOriginalCoordinate
{
    [self _readOriginalCoordinate];
    return _originalCoordinate != nil;
}
- (GEOLatLng *)originalCoordinate
{
    [self _readOriginalCoordinate];
    return _originalCoordinate;
}
- (void)setOriginalCoordinate:(GEOLatLng *)v
{
    _flags.read_originalCoordinate = 1;
    _flags.wrote_originalCoordinate = 1;
    _originalCoordinate = v;
}

- (void)_readCorrectedCoordinate {
    if (_flags.read_correctedCoordinate) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_correctedCoordinate) {
            static const uint32_t tags[] = {2, 0};
            GEORPCorrectedCoordinateReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasCorrectedCoordinate
{
    [self _readCorrectedCoordinate];
    return _correctedCoordinate != nil;
}
- (GEOLatLng *)correctedCoordinate
{
    [self _readCorrectedCoordinate];
    return _correctedCoordinate;
}
- (void)setCorrectedCoordinate:(GEOLatLng *)v
{
    _flags.read_correctedCoordinate = 1;
    _flags.wrote_correctedCoordinate = 1;
    _correctedCoordinate = v;
}


- (NSString *)description
{
    return [NSString stringWithFormat:@"%@ %@", [super description], [self dictionaryRepresentation]];
}

- (NSDictionary *)dictionaryRepresentation
{
    [self readAll:YES];
    NSMutableDictionary *dict = [NSMutableDictionary dictionary];
    {
        GEOLatLng * value = self.originalCoordinate;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"original_coordinate"];
        }
    }

    {
        GEOLatLng * value = self.correctedCoordinate;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"corrected_coordinate"];
        }
    }



    return dict;
}


#define TAG_PARSE_ALL_RECURSIVE     (UINT32_MAX)
#define TAG_PARSE_ALL_NON_RECURSIVE (UINT32_MAX-1)
#define TAG_PARSE_INITIAL           (UINT32_MAX-2)
#define TAG_UNKNOWN_FIELDS          (UINT32_MAX-3)

- (void)readAll:(BOOL)recursively {
    os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (reader == nil) return;
    @synchronized(reader) {
        static const uint32_t recursiveTag[] = {TAG_PARSE_ALL_RECURSIVE, 0};
        static const uint32_t nonRecursiveTag[] = {TAG_PARSE_ALL_NON_RECURSIVE, 0};
        GEORPCorrectedCoordinateReadSpecified(self, reader, recursively ? recursiveTag : nonRecursiveTag);
    }
}
BOOL GEORPCorrectedCoordinateReadAllFrom(__unsafe_unretained GEORPCorrectedCoordinate *self, __unsafe_unretained PBDataReader *reader, BOOL recursively) {
    if (self->_readerMarkLength != 0) {
        [NSException raise:NSInvalidArgumentException format:@"GEORPCorrectedCoordinateReadAllFrom can only be called once per object"];
        return NO;
    }

    if (reader.position > UINT32_MAX || reader.length > UINT32_MAX) {
        os_log_fault(OS_LOG_DEFAULT, "Cannot decode data with position/length > UINT32_MAX");
        return NO;
    }

    static const uint32_t recursiveTag[] = {TAG_PARSE_INITIAL, TAG_PARSE_ALL_RECURSIVE, 0};
    static const uint32_t initialTag[] = {TAG_PARSE_INITIAL, 0};

    @synchronized(reader) {
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = reader;
        os_unfair_lock_unlock(&(self->_readerLock));

        self->_readerMarkPos = (uint32_t)reader.position;
        self->_readerMarkLength = (uint32_t)reader.length;
        BOOL result = GEORPCorrectedCoordinateReadSpecified(self, reader, recursively ? recursiveTag : initialTag);
        return result;
    }
}

BOOL GEORPCorrectedCoordinateReadFrom(__unsafe_unretained GEORPCorrectedCoordinate *self, __unsafe_unretained PBDataReader *reader) {
    @synchronized(reader) {
        return GEORPCorrectedCoordinateReadAllFrom(self, reader, NO);
    }
}

BOOL GEORPCorrectedCoordinateReadSpecified(__unsafe_unretained GEORPCorrectedCoordinate *self, __unsafe_unretained PBDataReader *reader, const uint32_t *tags) {
    if (reader == nil) {
        return NO;
    }
    reader->_length = (NSUInteger)self->_readerMarkLength;
    [reader seekToOffset:(NSUInteger)self->_readerMarkPos];
    uint32_t readLazyCount = 0
        + self->_flags.read_originalCoordinate
        + self->_flags.read_correctedCoordinate
        ;
    #define requiredLazyFieldCount 1 // objc_msg_parse_all_threshold = 0.8

    BOOL readAll = NO;
    __attribute__((unused)) BOOL recursively = NO;
    BOOL isInitial = NO;
    size_t tagsRemaining = 0;
    for (; tags[tagsRemaining] != 0; ++tagsRemaining) {
        switch (tags[tagsRemaining]) {
            case TAG_PARSE_ALL_RECURSIVE:
                recursively = YES;
                readAll = YES;
                break;
            case TAG_PARSE_ALL_NON_RECURSIVE:
                readAll = YES;
                break;
            case TAG_PARSE_INITIAL:
                isInitial = YES;
                break;
            default:
                ++readLazyCount;
                break;
            }
    }
    if (!readAll && readLazyCount >= requiredLazyFieldCount) {
        readAll = YES;
    }
    const BOOL stopWhenNoMoreTagsRemaining = !readAll;
    if (readAll || isInitial) tags = NULL;

    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }
        
        BOOL shouldParseTag = readAll;
        for (size_t i = 0; tags != NULL && !shouldParseTag && tags[i] != 0; ++i) {
            shouldParseTag = (tags[i] == tag);
        }

        switch (tag) {

            case 1 /* originalCoordinate */:
            {
                if (!shouldParseTag || self->_flags.read_originalCoordinate) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_originalCoordinate) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEOLatLng *new_originalCoordinate = [[GEOLatLng alloc] init];
                self->_originalCoordinate = new_originalCoordinate;
                PBDataReaderMark mark_originalCoordinate;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_originalCoordinate);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEOLatLngReadAllFrom(new_originalCoordinate, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_originalCoordinate);

                --tagsRemaining;
            }
            break;
            case 2 /* correctedCoordinate */:
            {
                if (!shouldParseTag || self->_flags.read_correctedCoordinate) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_correctedCoordinate) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEOLatLng *new_correctedCoordinate = [[GEOLatLng alloc] init];
                self->_correctedCoordinate = new_correctedCoordinate;
                PBDataReaderMark mark_correctedCoordinate;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_correctedCoordinate);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEOLatLngReadAllFrom(new_correctedCoordinate, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_correctedCoordinate);

                --tagsRemaining;
            }
            break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    return NO;
                break;
        }
        if (stopWhenNoMoreTagsRemaining && tagsRemaining == 0) {
            break;
        }
    }


    if (readAll) {
        if (recursively) {
            [self->_originalCoordinate readAll:YES];
            [self->_correctedCoordinate readAll:YES];
        }
        self->_flags.read_originalCoordinate = YES;
        self->_flags.read_correctedCoordinate = YES;
    } else {
        for (size_t i = 0; tags != NULL && tags[i] != 0; ++i) {
            switch (tags[i]) {
                case 1 /* originalCoordinate */:
                    self->_flags.read_originalCoordinate = YES;
                    break;
                case 2 /* correctedCoordinate */:
                    self->_flags.read_correctedCoordinate = YES;
                    break;
            }
        }
    }

    #undef requiredLazyFieldCount
    if (readAll) {
        // reader is __unsafe_unretained
        const BOOL hadError = PBReaderHasError(reader);
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = nil;
        os_unfair_lock_unlock(&(self->_readerLock));
        return !hadError;
    }
    return !PBReaderHasError(reader);
}

+ (BOOL)isValid:(__unsafe_unretained PBDataReader *)reader
{
    return GEORPCorrectedCoordinateIsValid(reader);
}

BOOL GEORPCorrectedCoordinateIsValid(__unsafe_unretained PBDataReader *reader) {
    PBDataReaderMark resetMark;
    resetMark.pos = reader.position;
    resetMark.length = reader.length;
    #define returnValue(X) do { \
        const BOOL v = X; \
        PBReaderRecallMark(reader, &resetMark); \
        return v; \
    } while(0)
    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }

        switch (tag) {

            case 1 /* originalCoordinate */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_originalCoordinate;
                    if (!PBReaderPlaceMark(reader, &mark_originalCoordinate))
                        returnValue(NO);
                    if (!GEOLatLngIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_originalCoordinate);
                }
                break;
            case 2 /* correctedCoordinate */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_correctedCoordinate;
                    if (!PBReaderPlaceMark(reader, &mark_correctedCoordinate))
                        returnValue(NO);
                    if (!GEOLatLngIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_correctedCoordinate);
                }
                break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
        }
    }
    returnValue(!PBReaderHasError(reader));
    #undef returnValue
}

- (BOOL)readFrom:(PBDataReader *)reader
{
    return GEORPCorrectedCoordinateReadAllFrom(self, reader, NO);
}

- (void)writeTo:(PBDataWriter *)writer
{
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            if (!GEORPCorrectedCoordinateIsDirty(self)) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                uint32_t length = (_readerMarkLength - _readerMarkPos);
                NSData *data = [reader readBytes:length];
                [writer writeData:data];
                return;
            }
        }
    }

    [self readAll:NO];

    /* originalCoordinate */
    {
        if (self->_originalCoordinate != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_originalCoordinate, 1);
        }
    }

    /* correctedCoordinate */
    {
        if (self->_correctedCoordinate != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_correctedCoordinate, 2);
        }
    }
}

BOOL GEORPCorrectedCoordinateIsDirty(__unsafe_unretained GEORPCorrectedCoordinate *self)
{
    if (self == nil) {
        return NO;
    }
    
    BOOL dirty = NO
        || self->_flags.wrote_originalCoordinate
        || self->_flags.read_originalCoordinate
        || self->_flags.wrote_correctedCoordinate
        || self->_flags.read_correctedCoordinate
    ;


    return dirty;
}


- (void)copyTo:(GEORPCorrectedCoordinate *)other
{
    [self readAll:NO];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    other->_reader = _reader;
    other->_readerMarkPos = _readerMarkPos;
    other->_readerMarkLength = _readerMarkLength;
    os_unfair_lock_unlock(&_readerLock);
    if (_originalCoordinate)
    {
        other.originalCoordinate = _originalCoordinate;
    }
    if (_correctedCoordinate)
    {
        other.correctedCoordinate = _correctedCoordinate;
    }
}

- (id)copyWithZone:(NSZone *)zone
{
    GEORPCorrectedCoordinate *copy = [[[self class] allocWithZone:zone] init];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            BOOL wroteAny = NO
                || _flags.wrote_originalCoordinate
                || _flags.wrote_correctedCoordinate
            ;
            if (!wroteAny) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                GEORPCorrectedCoordinateReadAllFrom(copy, reader, NO);
                return copy;
            }
            [self readAll:NO];
        }
    }
    copy->_originalCoordinate = [_originalCoordinate copyWithZone:zone];
    copy->_correctedCoordinate = [_correctedCoordinate copyWithZone:zone];
    return copy;
}


- (BOOL)isEqual:(id)object
{
    GEORPCorrectedCoordinate *other = (GEORPCorrectedCoordinate *)object;
    if (![other isMemberOfClass:[self class]]) {
        return NO;
    }
    [self readAll:YES];
    [other readAll:YES];


    return YES
    &&
    ((!self->_originalCoordinate && !other->_originalCoordinate) || [self->_originalCoordinate isEqual:other->_originalCoordinate])
    &&
    ((!self->_correctedCoordinate && !other->_correctedCoordinate) || [self->_correctedCoordinate isEqual:other->_correctedCoordinate])
    ;
}

- (NSUInteger)hash
{
    [self readAll:YES];
    NSUInteger value = 0;

    return value
    ^
    [self->_originalCoordinate hash]
    ^
    [self->_correctedCoordinate hash]
    ;
}

- (void)mergeFrom:(GEORPCorrectedCoordinate *)other
{
    [other readAll:NO];
    if (self->_originalCoordinate && other->_originalCoordinate)
    {
        [self->_originalCoordinate mergeFrom:other->_originalCoordinate];
    }
    else if (!self->_originalCoordinate && other->_originalCoordinate)
    {
        [self setOriginalCoordinate:other->_originalCoordinate];
    }
    if (self->_correctedCoordinate && other->_correctedCoordinate)
    {
        [self->_correctedCoordinate mergeFrom:other->_correctedCoordinate];
    }
    else if (!self->_correctedCoordinate && other->_correctedCoordinate)
    {
        [self setCorrectedCoordinate:other->_correctedCoordinate];
    }
}


@end


