// This file was automatically generated by gyb
// DO NOT EDIT!
// Compiled from community_feedback.proto

#import "GEORPAddressCorrections.h"
#import <ProtocolBuffer/PBConstants.h>
#import <ProtocolBuffer/PBHashUtil.h>
#import <ProtocolBuffer/PBDataReader.h>
#import <os/lock_private.h>

#import <GeoServices/GEORPAccessPointCorrections.h>
#import <GeoServices/GEORPCorrectedCoordinate.h>
#import <GeoServices/GEORPFeedbackAddressFields.h>
#import <GeoServices/GEORPMapLocation.h>

#if !__has_feature(objc_arc)
# error This generated file depends on ARC but it is not enabled
#endif

@implementation GEORPAddressCorrections

- (instancetype)init
{
    self = [super init];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (instancetype)initWithData:(NSData *)data
{
    self = [super initWithData:data];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (void)_readAddressFields {
    if (_flags.read_addressFields) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_addressFields) {
            static const uint32_t tags[] = {2, 0};
            GEORPAddressCorrectionsReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasAddressFields
{
    [self _readAddressFields];
    return _addressFields != nil;
}
- (GEORPFeedbackAddressFields *)addressFields
{
    [self _readAddressFields];
    return _addressFields;
}
- (void)setAddressFields:(GEORPFeedbackAddressFields *)v
{
    _flags.read_addressFields = 1;
    _flags.wrote_addressFields = 1;
    _addressFields = v;
}

- (void)_readCoordinate {
    if (_flags.read_coordinate) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_coordinate) {
            static const uint32_t tags[] = {3, 0};
            GEORPAddressCorrectionsReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasCoordinate
{
    [self _readCoordinate];
    return _coordinate != nil;
}
- (GEORPCorrectedCoordinate *)coordinate
{
    [self _readCoordinate];
    return _coordinate;
}
- (void)setCoordinate:(GEORPCorrectedCoordinate *)v
{
    _flags.read_coordinate = 1;
    _flags.wrote_coordinate = 1;
    _coordinate = v;
}

- (void)_readMapLocation {
    if (_flags.read_mapLocation) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_mapLocation) {
            static const uint32_t tags[] = {4, 0};
            GEORPAddressCorrectionsReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasMapLocation
{
    [self _readMapLocation];
    return _mapLocation != nil;
}
- (GEORPMapLocation *)mapLocation
{
    [self _readMapLocation];
    return _mapLocation;
}
- (void)setMapLocation:(GEORPMapLocation *)v
{
    _flags.read_mapLocation = 1;
    _flags.wrote_mapLocation = 1;
    _mapLocation = v;
}

- (void)_readAccessPoint {
    if (_flags.read_accessPoint) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_accessPoint) {
            static const uint32_t tags[] = {5, 0};
            GEORPAddressCorrectionsReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasAccessPoint
{
    [self _readAccessPoint];
    return _accessPoint != nil;
}
- (GEORPAccessPointCorrections *)accessPoint
{
    [self _readAccessPoint];
    return _accessPoint;
}
- (void)setAccessPoint:(GEORPAccessPointCorrections *)v
{
    _flags.read_accessPoint = 1;
    _flags.wrote_accessPoint = 1;
    _accessPoint = v;
}

- (void)_readOriginalAddressFields {
    if (_flags.read_originalAddressFields) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_originalAddressFields) {
            static const uint32_t tags[] = {6, 0};
            GEORPAddressCorrectionsReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasOriginalAddressFields
{
    [self _readOriginalAddressFields];
    return _originalAddressFields != nil;
}
- (GEORPFeedbackAddressFields *)originalAddressFields
{
    [self _readOriginalAddressFields];
    return _originalAddressFields;
}
- (void)setOriginalAddressFields:(GEORPFeedbackAddressFields *)v
{
    _flags.read_originalAddressFields = 1;
    _flags.wrote_originalAddressFields = 1;
    _originalAddressFields = v;
}


- (NSString *)description
{
    return [NSString stringWithFormat:@"%@ %@", [super description], [self dictionaryRepresentation]];
}

- (NSDictionary *)dictionaryRepresentation
{
    [self readAll:YES];
    NSMutableDictionary *dict = [NSMutableDictionary dictionary];
    {
        GEORPFeedbackAddressFields * value = self.addressFields;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"address_fields"];
        }
    }

    {
        GEORPCorrectedCoordinate * value = self.coordinate;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"coordinate"];
        }
    }

    {
        GEORPMapLocation * value = self.mapLocation;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"map_location"];
        }
    }

    {
        GEORPAccessPointCorrections * value = self.accessPoint;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"access_point"];
        }
    }

    {
        GEORPFeedbackAddressFields * value = self.originalAddressFields;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"original_address_fields"];
        }
    }


    if (_unknownFields)
        [dict setObject:[_unknownFields dictionaryRepresentation] forKey:@"Unknown Fields"];

    return dict;
}


#define TAG_PARSE_ALL_RECURSIVE     (UINT32_MAX)
#define TAG_PARSE_ALL_NON_RECURSIVE (UINT32_MAX-1)
#define TAG_PARSE_INITIAL           (UINT32_MAX-2)
#define TAG_UNKNOWN_FIELDS          (UINT32_MAX-3)

- (void)readAll:(BOOL)recursively {
    os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (reader == nil) return;
    @synchronized(reader) {
        static const uint32_t recursiveTag[] = {TAG_PARSE_ALL_RECURSIVE, 0};
        static const uint32_t nonRecursiveTag[] = {TAG_PARSE_ALL_NON_RECURSIVE, 0};
        GEORPAddressCorrectionsReadSpecified(self, reader, recursively ? recursiveTag : nonRecursiveTag);
    }
}
BOOL GEORPAddressCorrectionsReadAllFrom(__unsafe_unretained GEORPAddressCorrections *self, __unsafe_unretained PBDataReader *reader, BOOL recursively) {
    if (self->_readerMarkLength != 0) {
        [NSException raise:NSInvalidArgumentException format:@"GEORPAddressCorrectionsReadAllFrom can only be called once per object"];
        return NO;
    }

    if (reader.position > UINT32_MAX || reader.length > UINT32_MAX) {
        os_log_fault(OS_LOG_DEFAULT, "Cannot decode data with position/length > UINT32_MAX");
        return NO;
    }

    static const uint32_t recursiveTag[] = {TAG_PARSE_INITIAL, TAG_PARSE_ALL_RECURSIVE, 0};
    static const uint32_t initialTag[] = {TAG_PARSE_INITIAL, 0};

    @synchronized(reader) {
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = reader;
        os_unfair_lock_unlock(&(self->_readerLock));

        self->_readerMarkPos = (uint32_t)reader.position;
        self->_readerMarkLength = (uint32_t)reader.length;
        BOOL result = GEORPAddressCorrectionsReadSpecified(self, reader, recursively ? recursiveTag : initialTag);
        return result;
    }
}

BOOL GEORPAddressCorrectionsReadFrom(__unsafe_unretained GEORPAddressCorrections *self, __unsafe_unretained PBDataReader *reader) {
    @synchronized(reader) {
        return GEORPAddressCorrectionsReadAllFrom(self, reader, NO);
    }
}

BOOL GEORPAddressCorrectionsReadSpecified(__unsafe_unretained GEORPAddressCorrections *self, __unsafe_unretained PBDataReader *reader, const uint32_t *tags) {
    if (reader == nil) {
        return NO;
    }
    reader->_length = (NSUInteger)self->_readerMarkLength;
    [reader seekToOffset:(NSUInteger)self->_readerMarkPos];
    uint32_t readLazyCount = 0
        + self->_flags.read_addressFields
        + self->_flags.read_coordinate
        + self->_flags.read_mapLocation
        + self->_flags.read_accessPoint
        + self->_flags.read_originalAddressFields
        + self->_flags.read_unknownFields
        ;
    #define requiredLazyFieldCount 4 // objc_msg_parse_all_threshold = 0.8

    BOOL readAll = NO;
    __attribute__((unused)) BOOL recursively = NO;
    BOOL shouldParseUnknown = NO;
    BOOL isInitial = NO;
    size_t tagsRemaining = 0;
    for (; tags[tagsRemaining] != 0; ++tagsRemaining) {
        switch (tags[tagsRemaining]) {
            case TAG_PARSE_ALL_RECURSIVE:
                recursively = YES;
                readAll = YES;
                shouldParseUnknown = YES;
                break;
            case TAG_PARSE_ALL_NON_RECURSIVE:
                readAll = YES;
                shouldParseUnknown = YES;
                break;
            case TAG_PARSE_INITIAL:
                isInitial = YES;
                break;
            case TAG_UNKNOWN_FIELDS:
                shouldParseUnknown = YES;
                ++readLazyCount;
                break;
            default:
                ++readLazyCount;
                break;
            }
    }
    if (!readAll && readLazyCount >= requiredLazyFieldCount) {
        readAll = YES;
        shouldParseUnknown = YES;
    }
    const BOOL stopWhenNoMoreTagsRemaining = !(readAll || shouldParseUnknown);
    if (readAll || isInitial) tags = NULL;

    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }
        
        BOOL shouldParseTag = readAll;
        for (size_t i = 0; tags != NULL && !shouldParseTag && tags[i] != 0; ++i) {
            shouldParseTag = (tags[i] == tag);
        }

        switch (tag) {

            case 2 /* addressFields */:
            {
                if (!shouldParseTag || self->_flags.read_addressFields) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_addressFields) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEORPFeedbackAddressFields *new_addressFields = [[GEORPFeedbackAddressFields alloc] init];
                self->_addressFields = new_addressFields;
                PBDataReaderMark mark_addressFields;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_addressFields);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEORPFeedbackAddressFieldsReadAllFrom(new_addressFields, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_addressFields);

                --tagsRemaining;
            }
            break;
            case 3 /* coordinate */:
            {
                if (!shouldParseTag || self->_flags.read_coordinate) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_coordinate) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEORPCorrectedCoordinate *new_coordinate = [[GEORPCorrectedCoordinate alloc] init];
                self->_coordinate = new_coordinate;
                PBDataReaderMark mark_coordinate;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_coordinate);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEORPCorrectedCoordinateReadAllFrom(new_coordinate, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_coordinate);

                --tagsRemaining;
            }
            break;
            case 4 /* mapLocation */:
            {
                if (!shouldParseTag || self->_flags.read_mapLocation) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_mapLocation) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEORPMapLocation *new_mapLocation = [[GEORPMapLocation alloc] init];
                self->_mapLocation = new_mapLocation;
                PBDataReaderMark mark_mapLocation;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_mapLocation);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEORPMapLocationReadAllFrom(new_mapLocation, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_mapLocation);

                --tagsRemaining;
            }
            break;
            case 5 /* accessPoint */:
            {
                if (!shouldParseTag || self->_flags.read_accessPoint) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_accessPoint) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEORPAccessPointCorrections *new_accessPoint = [[GEORPAccessPointCorrections alloc] init];
                self->_accessPoint = new_accessPoint;
                PBDataReaderMark mark_accessPoint;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_accessPoint);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEORPAccessPointCorrectionsReadAllFrom(new_accessPoint, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_accessPoint);

                --tagsRemaining;
            }
            break;
            case 6 /* originalAddressFields */:
            {
                if (!shouldParseTag || self->_flags.read_originalAddressFields) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_originalAddressFields) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEORPFeedbackAddressFields *new_originalAddressFields = [[GEORPFeedbackAddressFields alloc] init];
                self->_originalAddressFields = new_originalAddressFields;
                PBDataReaderMark mark_originalAddressFields;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_originalAddressFields);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEORPFeedbackAddressFieldsReadAllFrom(new_originalAddressFields, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_originalAddressFields);

                --tagsRemaining;
            }
            break;
            default:
                if (!shouldParseUnknown || self->_flags.read_unknownFields) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                if (!self->_unknownFields)
                    self->_unknownFields = [[PBUnknownFields alloc] init];
                if (!PBUnknownFieldAdd(self->_unknownFields, tag, aType, reader))
                    return NO;
                break;
        }
        if (stopWhenNoMoreTagsRemaining && tagsRemaining == 0) {
            break;
        }
    }

    if (shouldParseUnknown) {
        self->_flags.read_unknownFields = YES;
    }

    if (readAll) {
        if (recursively) {
            [self->_addressFields readAll:YES];
            [self->_coordinate readAll:YES];
            [self->_mapLocation readAll:YES];
            [self->_accessPoint readAll:YES];
            [self->_originalAddressFields readAll:YES];
        }
        self->_flags.read_addressFields = YES;
        self->_flags.read_coordinate = YES;
        self->_flags.read_mapLocation = YES;
        self->_flags.read_accessPoint = YES;
        self->_flags.read_originalAddressFields = YES;
    } else {
        for (size_t i = 0; tags != NULL && tags[i] != 0; ++i) {
            switch (tags[i]) {
                case 2 /* addressFields */:
                    self->_flags.read_addressFields = YES;
                    break;
                case 3 /* coordinate */:
                    self->_flags.read_coordinate = YES;
                    break;
                case 4 /* mapLocation */:
                    self->_flags.read_mapLocation = YES;
                    break;
                case 5 /* accessPoint */:
                    self->_flags.read_accessPoint = YES;
                    break;
                case 6 /* originalAddressFields */:
                    self->_flags.read_originalAddressFields = YES;
                    break;
            }
        }
    }

    #undef requiredLazyFieldCount
    if (readAll) {
        // reader is __unsafe_unretained
        const BOOL hadError = PBReaderHasError(reader);
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = nil;
        os_unfair_lock_unlock(&(self->_readerLock));
        return !hadError;
    }
    return !PBReaderHasError(reader);
}

+ (BOOL)isValid:(__unsafe_unretained PBDataReader *)reader
{
    return GEORPAddressCorrectionsIsValid(reader);
}

BOOL GEORPAddressCorrectionsIsValid(__unsafe_unretained PBDataReader *reader) {
    PBDataReaderMark resetMark;
    resetMark.pos = reader.position;
    resetMark.length = reader.length;
    #define returnValue(X) do { \
        const BOOL v = X; \
        PBReaderRecallMark(reader, &resetMark); \
        return v; \
    } while(0)
    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }

        switch (tag) {

            case 2 /* addressFields */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_addressFields;
                    if (!PBReaderPlaceMark(reader, &mark_addressFields))
                        returnValue(NO);
                    if (!GEORPFeedbackAddressFieldsIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_addressFields);
                }
                break;
            case 3 /* coordinate */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_coordinate;
                    if (!PBReaderPlaceMark(reader, &mark_coordinate))
                        returnValue(NO);
                    if (!GEORPCorrectedCoordinateIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_coordinate);
                }
                break;
            case 4 /* mapLocation */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_mapLocation;
                    if (!PBReaderPlaceMark(reader, &mark_mapLocation))
                        returnValue(NO);
                    if (!GEORPMapLocationIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_mapLocation);
                }
                break;
            case 5 /* accessPoint */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_accessPoint;
                    if (!PBReaderPlaceMark(reader, &mark_accessPoint))
                        returnValue(NO);
                    if (!GEORPAccessPointCorrectionsIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_accessPoint);
                }
                break;
            case 6 /* originalAddressFields */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_originalAddressFields;
                    if (!PBReaderPlaceMark(reader, &mark_originalAddressFields))
                        returnValue(NO);
                    if (!GEORPFeedbackAddressFieldsIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_originalAddressFields);
                }
                break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
        }
    }
    returnValue(!PBReaderHasError(reader));
    #undef returnValue
}

- (BOOL)readFrom:(PBDataReader *)reader
{
    return GEORPAddressCorrectionsReadAllFrom(self, reader, NO);
}

- (void)writeTo:(PBDataWriter *)writer
{
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            if (!GEORPAddressCorrectionsIsDirty(self)) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                uint32_t length = (_readerMarkLength - _readerMarkPos);
                NSData *data = [reader readBytes:length];
                [writer writeData:data];
                return;
            }
        }
    }

    [self readAll:NO];

    /* addressFields */
    {
        if (self->_addressFields != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_addressFields, 2);
        }
    }

    /* coordinate */
    {
        if (self->_coordinate != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_coordinate, 3);
        }
    }

    /* mapLocation */
    {
        if (self->_mapLocation != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_mapLocation, 4);
        }
    }

    /* accessPoint */
    {
        if (self->_accessPoint != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_accessPoint, 5);
        }
    }

    /* originalAddressFields */
    {
        if (self->_originalAddressFields != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_originalAddressFields, 6);
        }
    }
    /* Unknown Fields */
    {
        [_unknownFields writeTo:writer];
    }
}

BOOL GEORPAddressCorrectionsIsDirty(__unsafe_unretained GEORPAddressCorrections *self)
{
    if (self == nil) {
        return NO;
    }
    
    BOOL dirty = NO
        || self->_flags.wrote_addressFields
        || (self->_flags.read_addressFields && GEORPFeedbackAddressFieldsIsDirty(self->_addressFields))
        || self->_flags.wrote_coordinate
        || (self->_flags.read_coordinate && GEORPCorrectedCoordinateIsDirty(self->_coordinate))
        || self->_flags.wrote_mapLocation
        || (self->_flags.read_mapLocation && GEORPMapLocationIsDirty(self->_mapLocation))
        || self->_flags.wrote_accessPoint
        || (self->_flags.read_accessPoint && GEORPAccessPointCorrectionsIsDirty(self->_accessPoint))
        || self->_flags.wrote_originalAddressFields
        || (self->_flags.read_originalAddressFields && GEORPFeedbackAddressFieldsIsDirty(self->_originalAddressFields))
        || self->_flags.wrote_unknownFields
    ;


    return dirty;
}


- (void)copyTo:(GEORPAddressCorrections *)other
{
    [self readAll:NO];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    other->_reader = _reader;
    other->_readerMarkPos = _readerMarkPos;
    other->_readerMarkLength = _readerMarkLength;
    os_unfair_lock_unlock(&_readerLock);
    if (_addressFields)
    {
        other.addressFields = _addressFields;
    }
    if (_coordinate)
    {
        other.coordinate = _coordinate;
    }
    if (_mapLocation)
    {
        other.mapLocation = _mapLocation;
    }
    if (_accessPoint)
    {
        other.accessPoint = _accessPoint;
    }
    if (_originalAddressFields)
    {
        other.originalAddressFields = _originalAddressFields;
    }
}

- (id)copyWithZone:(NSZone *)zone
{
    GEORPAddressCorrections *copy = [[[self class] allocWithZone:zone] init];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            BOOL wroteAny = NO
                || _flags.wrote_addressFields
                || _flags.wrote_coordinate
                || _flags.wrote_mapLocation
                || _flags.wrote_accessPoint
                || _flags.wrote_originalAddressFields
                || _flags.wrote_unknownFields
            ;
            if (!wroteAny) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                GEORPAddressCorrectionsReadAllFrom(copy, reader, NO);
                return copy;
            }
            [self readAll:NO];
        }
    }
    copy->_addressFields = [_addressFields copyWithZone:zone];
    copy->_coordinate = [_coordinate copyWithZone:zone];
    copy->_mapLocation = [_mapLocation copyWithZone:zone];
    copy->_accessPoint = [_accessPoint copyWithZone:zone];
    copy->_originalAddressFields = [_originalAddressFields copyWithZone:zone];
    copy->_unknownFields = _unknownFields;
    return copy;
}


- (BOOL)isEqual:(id)object
{
    GEORPAddressCorrections *other = (GEORPAddressCorrections *)object;
    if (![other isMemberOfClass:[self class]]) {
        return NO;
    }
    [self readAll:YES];
    [other readAll:YES];


    return YES
    &&
    ((!self->_addressFields && !other->_addressFields) || [self->_addressFields isEqual:other->_addressFields])
    &&
    ((!self->_coordinate && !other->_coordinate) || [self->_coordinate isEqual:other->_coordinate])
    &&
    ((!self->_mapLocation && !other->_mapLocation) || [self->_mapLocation isEqual:other->_mapLocation])
    &&
    ((!self->_accessPoint && !other->_accessPoint) || [self->_accessPoint isEqual:other->_accessPoint])
    &&
    ((!self->_originalAddressFields && !other->_originalAddressFields) || [self->_originalAddressFields isEqual:other->_originalAddressFields])
    ;
}

- (NSUInteger)hash
{
    [self readAll:YES];
    NSUInteger value = 0;

    return value
    ^
    [self->_addressFields hash]
    ^
    [self->_coordinate hash]
    ^
    [self->_mapLocation hash]
    ^
    [self->_accessPoint hash]
    ^
    [self->_originalAddressFields hash]
    ;
}

- (void)mergeFrom:(GEORPAddressCorrections *)other
{
    [other readAll:NO];
    if (self->_addressFields && other->_addressFields)
    {
        [self->_addressFields mergeFrom:other->_addressFields];
    }
    else if (!self->_addressFields && other->_addressFields)
    {
        [self setAddressFields:other->_addressFields];
    }
    if (self->_coordinate && other->_coordinate)
    {
        [self->_coordinate mergeFrom:other->_coordinate];
    }
    else if (!self->_coordinate && other->_coordinate)
    {
        [self setCoordinate:other->_coordinate];
    }
    if (self->_mapLocation && other->_mapLocation)
    {
        [self->_mapLocation mergeFrom:other->_mapLocation];
    }
    else if (!self->_mapLocation && other->_mapLocation)
    {
        [self setMapLocation:other->_mapLocation];
    }
    if (self->_accessPoint && other->_accessPoint)
    {
        [self->_accessPoint mergeFrom:other->_accessPoint];
    }
    else if (!self->_accessPoint && other->_accessPoint)
    {
        [self setAccessPoint:other->_accessPoint];
    }
    if (self->_originalAddressFields && other->_originalAddressFields)
    {
        [self->_originalAddressFields mergeFrom:other->_originalAddressFields];
    }
    else if (!self->_originalAddressFields && other->_originalAddressFields)
    {
        [self setOriginalAddressFields:other->_originalAddressFields];
    }
}

- (PBUnknownFields *)unknownFields
{
    if (_flags.read_unknownFields) return _unknownFields;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return _unknownFields;
    @synchronized(reader) {
        if (!_flags.read_unknownFields) {
            static const uint32_t tags[] = {TAG_UNKNOWN_FIELDS, 0};
            GEORPAddressCorrectionsReadSpecified(self, reader, tags);
        }
    }
    return _unknownFields;
}
- (void)clearUnknownFields:(BOOL)recursively
{
    _flags.read_unknownFields = 1;
    _flags.wrote_unknownFields = 1;
    _unknownFields = nil;
    if (!recursively) {
        return;
    }
    [self readAll:NO];
    [_addressFields clearUnknownFields:YES];
    [_accessPoint clearUnknownFields:YES];
    [_originalAddressFields clearUnknownFields:YES];
}

@end


