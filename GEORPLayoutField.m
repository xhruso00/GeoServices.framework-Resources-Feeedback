// This file was automatically generated by gyb
// DO NOT EDIT!
// Compiled from community_feedback.proto

#import "GEORPLayoutField.h"
#import <ProtocolBuffer/PBConstants.h>
#import <ProtocolBuffer/PBHashUtil.h>
#import <ProtocolBuffer/PBDataReader.h>
#import <os/lock_private.h>

#import <GeoServices/GEORPFeedbackLayoutFieldName.h>

#if !__has_feature(objc_arc)
# error This generated file depends on ARC but it is not enabled
#endif

@implementation GEORPLayoutField

- (instancetype)init
{
    self = [super init];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (instancetype)initWithData:(NSData *)data
{
    self = [super initWithData:data];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (GEORPFeedbackLayoutFieldType)type
{
    return _flags.has_type ? _type : GEORPFeedbackLayoutFieldType_FEEDBACK_LAYOUT_FIELD_TYPE_UNKNOWN;
}
- (void)setType:(GEORPFeedbackLayoutFieldType)v
{
    _flags.wrote_type = 1;
    _flags.has_type = 1;
    _type = v;
}
- (void)setHasType:(BOOL)f
{
    _flags.wrote_type = 1;
    _flags.has_type = f ? 1 : 0;
}
- (BOOL)hasType
{
    return (BOOL)_flags.has_type;
}
- (NSString *)typeAsString:(GEORPFeedbackLayoutFieldType)value
{
    return GEORPFeedbackLayoutFieldTypeAsString(value);
}
- (GEORPFeedbackLayoutFieldType)StringAsType:(NSString *)str
{
    return StringAsGEORPFeedbackLayoutFieldType(str);
}

- (void)_readName {
    if (_flags.read_name) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_name) {
            static const uint32_t tags[] = {2, 0};
            GEORPLayoutFieldReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasName
{
    [self _readName];
    return _name != nil;
}
- (GEORPFeedbackLayoutFieldName *)name
{
    [self _readName];
    return _name;
}
- (void)setName:(GEORPFeedbackLayoutFieldName *)v
{
    _flags.read_name = 1;
    _flags.wrote_name = 1;
    _name = v;
}

- (void)_readDisplayText {
    if (_flags.read_displayText) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_displayText) {
            static const uint32_t tags[] = {3, 0};
            GEORPLayoutFieldReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasDisplayText
{
    [self _readDisplayText];
    return _displayText != nil;
}
- (NSString *)displayText
{
    [self _readDisplayText];
    return _displayText;
}
- (void)setDisplayText:(NSString *)v
{
    _flags.read_displayText = 1;
    _flags.wrote_displayText = 1;
    _displayText = v;
}

- (BOOL)enabled
{
    return _enabled;
}
- (void)setEnabled:(BOOL)v
{
    _flags.wrote_enabled = 1;
    _flags.has_enabled = 1;
    _enabled = v;
}
- (void)setHasEnabled:(BOOL)f
{
    _flags.wrote_enabled = 1;
    _flags.has_enabled = f ? 1 : 0;
}
- (BOOL)hasEnabled
{
    return (BOOL)_flags.has_enabled;
}

- (uint32_t)ordinal
{
    return _ordinal;
}
- (void)setOrdinal:(uint32_t)v
{
    _flags.wrote_ordinal = 1;
    _flags.has_ordinal = 1;
    _ordinal = v;
}
- (void)setHasOrdinal:(BOOL)f
{
    _flags.wrote_ordinal = 1;
    _flags.has_ordinal = f ? 1 : 0;
}
- (BOOL)hasOrdinal
{
    return (BOOL)_flags.has_ordinal;
}


- (NSString *)description
{
    return [NSString stringWithFormat:@"%@ %@", [super description], [self dictionaryRepresentation]];
}

- (NSDictionary *)dictionaryRepresentation
{
    [self readAll:YES];
    NSMutableDictionary *dict = [NSMutableDictionary dictionary];
    if (self->_flags.has_type)
    {
        [dict setObject:GEORPFeedbackLayoutFieldTypeAsString(self->_type) forKey:@"type"];
    }

    {
        GEORPFeedbackLayoutFieldName * value = self.name;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"name"];
        }
    }

    {
        NSString * value = self.displayText;
        if (value) {
            [dict setObject:value forKey:@"display_text"];
        }
    }

    if (self->_flags.has_enabled)
    {
        [dict setObject:@(self->_enabled) forKey:@"enabled"];
    }

    if (self->_flags.has_ordinal)
    {
        [dict setObject:@(self->_ordinal) forKey:@"ordinal"];
    }


    if (_unknownFields)
        [dict setObject:[_unknownFields dictionaryRepresentation] forKey:@"Unknown Fields"];

    return dict;
}


#define TAG_PARSE_ALL_RECURSIVE     (UINT32_MAX)
#define TAG_PARSE_ALL_NON_RECURSIVE (UINT32_MAX-1)
#define TAG_PARSE_INITIAL           (UINT32_MAX-2)
#define TAG_UNKNOWN_FIELDS          (UINT32_MAX-3)

- (void)readAll:(BOOL)recursively {
    os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (reader == nil) return;
    @synchronized(reader) {
        static const uint32_t recursiveTag[] = {TAG_PARSE_ALL_RECURSIVE, 0};
        static const uint32_t nonRecursiveTag[] = {TAG_PARSE_ALL_NON_RECURSIVE, 0};
        GEORPLayoutFieldReadSpecified(self, reader, recursively ? recursiveTag : nonRecursiveTag);
    }
}
BOOL GEORPLayoutFieldReadAllFrom(__unsafe_unretained GEORPLayoutField *self, __unsafe_unretained PBDataReader *reader, BOOL recursively) {
    if (self->_readerMarkLength != 0) {
        [NSException raise:NSInvalidArgumentException format:@"GEORPLayoutFieldReadAllFrom can only be called once per object"];
        return NO;
    }

    if (reader.position > UINT32_MAX || reader.length > UINT32_MAX) {
        os_log_fault(OS_LOG_DEFAULT, "Cannot decode data with position/length > UINT32_MAX");
        return NO;
    }

    static const uint32_t recursiveTag[] = {TAG_PARSE_INITIAL, TAG_PARSE_ALL_RECURSIVE, 0};
    static const uint32_t initialTag[] = {TAG_PARSE_INITIAL, 0};

    @synchronized(reader) {
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = reader;
        os_unfair_lock_unlock(&(self->_readerLock));

        self->_readerMarkPos = (uint32_t)reader.position;
        self->_readerMarkLength = (uint32_t)reader.length;
        BOOL result = GEORPLayoutFieldReadSpecified(self, reader, recursively ? recursiveTag : initialTag);
        return result;
    }
}

BOOL GEORPLayoutFieldReadFrom(__unsafe_unretained GEORPLayoutField *self, __unsafe_unretained PBDataReader *reader) {
    @synchronized(reader) {
        return GEORPLayoutFieldReadAllFrom(self, reader, NO);
    }
}

BOOL GEORPLayoutFieldReadSpecified(__unsafe_unretained GEORPLayoutField *self, __unsafe_unretained PBDataReader *reader, const uint32_t *tags) {
    if (reader == nil) {
        return NO;
    }
    reader->_length = (NSUInteger)self->_readerMarkLength;
    [reader seekToOffset:(NSUInteger)self->_readerMarkPos];
    uint32_t readLazyCount = 0
        + self->_flags.read_name
        + self->_flags.read_displayText
        + self->_flags.read_unknownFields
        ;
    #define requiredLazyFieldCount 1 // objc_msg_parse_all_threshold = 0.8

    BOOL readAll = NO;
    __attribute__((unused)) BOOL recursively = NO;
    BOOL shouldParseUnknown = NO;
    BOOL isInitial = NO;
    size_t tagsRemaining = 0;
    for (; tags[tagsRemaining] != 0; ++tagsRemaining) {
        switch (tags[tagsRemaining]) {
            case TAG_PARSE_ALL_RECURSIVE:
                recursively = YES;
                readAll = YES;
                shouldParseUnknown = YES;
                break;
            case TAG_PARSE_ALL_NON_RECURSIVE:
                readAll = YES;
                shouldParseUnknown = YES;
                break;
            case TAG_PARSE_INITIAL:
                isInitial = YES;
                break;
            case TAG_UNKNOWN_FIELDS:
                shouldParseUnknown = YES;
                ++readLazyCount;
                break;
            default:
                ++readLazyCount;
                break;
            }
    }
    if (!readAll && readLazyCount >= requiredLazyFieldCount) {
        readAll = YES;
        shouldParseUnknown = YES;
    }
    const BOOL stopWhenNoMoreTagsRemaining = !(readAll || shouldParseUnknown);
    if (readAll || isInitial) tags = NULL;

    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }
        
        BOOL shouldParseTag = readAll;
        for (size_t i = 0; tags != NULL && !shouldParseTag && tags[i] != 0; ++i) {
            shouldParseTag = (tags[i] == tag);
        }

        switch (tag) {

            case 1 /* type */:
            {
                if (!isInitial) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                self->_flags.has_type = YES;
                self->_type = PBReaderReadInt32(reader);

            }
            break;
            case 2 /* name */:
            {
                if (!shouldParseTag || self->_flags.read_name) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_name) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEORPFeedbackLayoutFieldName *new_name = [[GEORPFeedbackLayoutFieldName alloc] init];
                self->_name = new_name;
                PBDataReaderMark mark_name;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_name);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEORPFeedbackLayoutFieldNameReadAllFrom(new_name, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_name);

                --tagsRemaining;
            }
            break;
            case 3 /* displayText */:
            {
                if (!shouldParseTag || self->_flags.read_displayText) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_displayText) {
                        --tagsRemaining;
                    }
                    break;
                }
                NSString * new_displayText = PBReaderReadString(reader);
                self->_displayText = new_displayText;

                --tagsRemaining;
            }
            break;
            case 4 /* enabled */:
            {
                if (!isInitial) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                self->_flags.has_enabled = YES;
                self->_enabled = PBReaderReadBOOL(reader);

            }
            break;
            case 5 /* ordinal */:
            {
                if (!isInitial) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                self->_flags.has_ordinal = YES;
                self->_ordinal = PBReaderReadUint32(reader);

            }
            break;
            default:
                if (!shouldParseUnknown || self->_flags.read_unknownFields) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                if (!self->_unknownFields)
                    self->_unknownFields = [[PBUnknownFields alloc] init];
                if (!PBUnknownFieldAdd(self->_unknownFields, tag, aType, reader))
                    return NO;
                break;
        }
        if (stopWhenNoMoreTagsRemaining && tagsRemaining == 0) {
            break;
        }
    }

    if (shouldParseUnknown) {
        self->_flags.read_unknownFields = YES;
    }

    if (readAll) {
        if (recursively) {
            [self->_name readAll:YES];
        }
        self->_flags.read_name = YES;
        self->_flags.read_displayText = YES;
    } else {
        for (size_t i = 0; tags != NULL && tags[i] != 0; ++i) {
            switch (tags[i]) {
                case 2 /* name */:
                    self->_flags.read_name = YES;
                    break;
                case 3 /* displayText */:
                    self->_flags.read_displayText = YES;
                    break;
            }
        }
    }

    #undef requiredLazyFieldCount
    if (readAll) {
        // reader is __unsafe_unretained
        const BOOL hadError = PBReaderHasError(reader);
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = nil;
        os_unfair_lock_unlock(&(self->_readerLock));
        return !hadError;
    }
    return !PBReaderHasError(reader);
}

+ (BOOL)isValid:(__unsafe_unretained PBDataReader *)reader
{
    return GEORPLayoutFieldIsValid(reader);
}

BOOL GEORPLayoutFieldIsValid(__unsafe_unretained PBDataReader *reader) {
    PBDataReaderMark resetMark;
    resetMark.pos = reader.position;
    resetMark.length = reader.length;
    #define returnValue(X) do { \
        const BOOL v = X; \
        PBReaderRecallMark(reader, &resetMark); \
        return v; \
    } while(0)
    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }

        switch (tag) {

            case 1 /* type */:
                if (TYPE_VARINT != aType)
                    returnValue(NO);
                PBReaderReadInt32(reader);
                break;
            case 2 /* name */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_name;
                    if (!PBReaderPlaceMark(reader, &mark_name))
                        returnValue(NO);
                    if (!GEORPFeedbackLayoutFieldNameIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_name);
                }
                break;
            case 3 /* displayText */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
                break;
            case 4 /* enabled */:
                if (TYPE_VARINT != aType)
                    returnValue(NO);
                PBReaderReadBOOL(reader);
                break;
            case 5 /* ordinal */:
                if (TYPE_VARINT != aType)
                    returnValue(NO);
                PBReaderReadUint32(reader);
                break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
        }
    }
    returnValue(!PBReaderHasError(reader));
    #undef returnValue
}

- (BOOL)readFrom:(PBDataReader *)reader
{
    return GEORPLayoutFieldReadAllFrom(self, reader, NO);
}

- (void)writeTo:(PBDataWriter *)writer
{
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            if (!GEORPLayoutFieldIsDirty(self)) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                uint32_t length = (_readerMarkLength - _readerMarkPos);
                NSData *data = [reader readBytes:length];
                [writer writeData:data];
                return;
            }
        }
    }

    [self readAll:NO];

    /* type */
    {
        if (self->_flags.has_type)
        {
            PBDataWriterWriteInt32Field(writer, self->_type, 1);
        }
    }

    /* name */
    {
        if (self->_name != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_name, 2);
        }
    }

    /* displayText */
    {
        if (self->_displayText)
        {
            PBDataWriterWriteStringField(writer, self->_displayText, 3);
        }
    }

    /* enabled */
    {
        if (self->_flags.has_enabled)
        {
            PBDataWriterWriteBOOLField(writer, self->_enabled, 4);
        }
    }

    /* ordinal */
    {
        if (self->_flags.has_ordinal)
        {
            PBDataWriterWriteUint32Field(writer, self->_ordinal, 5);
        }
    }
    /* Unknown Fields */
    {
        [_unknownFields writeTo:writer];
    }
}

BOOL GEORPLayoutFieldIsDirty(__unsafe_unretained GEORPLayoutField *self)
{
    if (self == nil) {
        return NO;
    }
    
    BOOL dirty = NO
        || self->_flags.wrote_type
        || self->_flags.wrote_name
        || self->_flags.read_name
        || self->_flags.wrote_displayText
        || self->_flags.read_displayText
        || self->_flags.wrote_enabled
        || self->_flags.wrote_ordinal
        || self->_flags.wrote_unknownFields
    ;


    return dirty;
}


- (void)copyTo:(GEORPLayoutField *)other
{
    [self readAll:NO];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    other->_reader = _reader;
    other->_readerMarkPos = _readerMarkPos;
    other->_readerMarkLength = _readerMarkLength;
    os_unfair_lock_unlock(&_readerLock);
    if (self->_flags.has_type)
    {
        other->_type = _type;
        other->_flags.has_type = YES;
    }
    if (_name)
    {
        other.name = _name;
    }
    if (_displayText)
    {
        other.displayText = _displayText;
    }
    if (self->_flags.has_enabled)
    {
        other->_enabled = _enabled;
        other->_flags.has_enabled = YES;
    }
    if (self->_flags.has_ordinal)
    {
        other->_ordinal = _ordinal;
        other->_flags.has_ordinal = YES;
    }
}

- (id)copyWithZone:(NSZone *)zone
{
    GEORPLayoutField *copy = [[[self class] allocWithZone:zone] init];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            BOOL wroteAny = NO
                || _flags.wrote_type
                || _flags.wrote_name
                || _flags.wrote_displayText
                || _flags.wrote_enabled
                || _flags.wrote_ordinal
                || _flags.wrote_unknownFields
            ;
            if (!wroteAny) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                GEORPLayoutFieldReadAllFrom(copy, reader, NO);
                return copy;
            }
            [self readAll:NO];
        }
    }
    if (self->_flags.has_type)
    {
        copy->_type = _type;
        copy->_flags.has_type = YES;
    }
    copy->_name = [_name copyWithZone:zone];
    copy->_displayText = [_displayText copyWithZone:zone];
    if (self->_flags.has_enabled)
    {
        copy->_enabled = _enabled;
        copy->_flags.has_enabled = YES;
    }
    if (self->_flags.has_ordinal)
    {
        copy->_ordinal = _ordinal;
        copy->_flags.has_ordinal = YES;
    }
    copy->_unknownFields = _unknownFields;
    return copy;
}


- (BOOL)isEqual:(id)object
{
    GEORPLayoutField *other = (GEORPLayoutField *)object;
    if (![other isMemberOfClass:[self class]]) {
        return NO;
    }
    [self readAll:YES];
    [other readAll:YES];


    return YES
    &&
    ((self->_flags.has_type && other->_flags.has_type && self->_type == other->_type) || (!self->_flags.has_type && !other->_flags.has_type))
    &&
    ((!self->_name && !other->_name) || [self->_name isEqual:other->_name])
    &&
    ((!self->_displayText && !other->_displayText) || [self->_displayText isEqual:other->_displayText])
    &&
    ((self->_flags.has_enabled && other->_flags.has_enabled && ((self->_enabled && other->_enabled) || (!self->_enabled && !other->_enabled))) || (!self->_flags.has_enabled && !other->_flags.has_enabled))
    &&
    ((self->_flags.has_ordinal && other->_flags.has_ordinal && self->_ordinal == other->_ordinal) || (!self->_flags.has_ordinal && !other->_flags.has_ordinal))
    ;
}

- (NSUInteger)hash
{
    [self readAll:YES];
    NSUInteger value = 0;

    return value
    ^
    (self->_flags.has_type ? PBHashInt((NSUInteger)self->_type) : 0)
    ^
    [self->_name hash]
    ^
    [self->_displayText hash]
    ^
    (self->_flags.has_enabled ? PBHashInt((NSUInteger)self->_enabled) : 0)
    ^
    (self->_flags.has_ordinal ? PBHashInt((NSUInteger)self->_ordinal) : 0)
    ;
}

- (void)mergeFrom:(GEORPLayoutField *)other
{
    [other readAll:NO];
    if (other->_flags.has_type)
    {
        self->_type = other->_type;
        self->_flags.has_type = 1;
    }
    if (self->_name && other->_name)
    {
        [self->_name mergeFrom:other->_name];
    }
    else if (!self->_name && other->_name)
    {
        [self setName:other->_name];
    }
    if (other->_displayText)
    {
        [self setDisplayText:other->_displayText];
    }
    if (other->_flags.has_enabled)
    {
        self->_enabled = other->_enabled;
        self->_flags.has_enabled = 1;
    }
    if (other->_flags.has_ordinal)
    {
        self->_ordinal = other->_ordinal;
        self->_flags.has_ordinal = 1;
    }
}

- (PBUnknownFields *)unknownFields
{
    if (_flags.read_unknownFields) return _unknownFields;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return _unknownFields;
    @synchronized(reader) {
        if (!_flags.read_unknownFields) {
            static const uint32_t tags[] = {TAG_UNKNOWN_FIELDS, 0};
            GEORPLayoutFieldReadSpecified(self, reader, tags);
        }
    }
    return _unknownFields;
}
- (void)clearUnknownFields:(BOOL)recursively
{
    _flags.read_unknownFields = 1;
    _flags.wrote_unknownFields = 1;
    _unknownFields = nil;
    if (!recursively) {
        return;
    }
    [self readAll:NO];
    [_name clearUnknownFields:YES];
}

@end


