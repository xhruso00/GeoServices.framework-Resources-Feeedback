// This file was automatically generated by gyb
// DO NOT EDIT!
// Compiled from reportaproblem.proto

#import "GEORPPlaceInfo.h"
#import <ProtocolBuffer/PBConstants.h>
#import <ProtocolBuffer/PBHashUtil.h>
#import <ProtocolBuffer/PBDataReader.h>
#import <os/lock_private.h>

#import <GeoServices/GEOPDPlaceRequest.h>
#import <GeoServices/GEOPDPlaceResponse.h>

#if !__has_feature(objc_arc)
# error This generated file depends on ARC but it is not enabled
#endif

@implementation GEORPPlaceInfo

- (instancetype)init
{
    self = [super init];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (instancetype)initWithData:(NSData *)data
{
    self = [super initWithData:data];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (void)_readPlaceRequest {
    if (_flags.read_placeRequest) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_placeRequest) {
            static const uint32_t tags[] = {1, 0};
            GEORPPlaceInfoReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasPlaceRequest
{
    [self _readPlaceRequest];
    return _placeRequest != nil;
}
- (GEOPDPlaceRequest *)placeRequest
{
    [self _readPlaceRequest];
    return _placeRequest;
}
- (void)setPlaceRequest:(GEOPDPlaceRequest *)v
{
    _flags.read_placeRequest = 1;
    _flags.wrote_placeRequest = 1;
    _placeRequest = v;
}

- (void)_readPlaceResponse {
    if (_flags.read_placeResponse) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_placeResponse) {
            static const uint32_t tags[] = {2, 0};
            GEORPPlaceInfoReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasPlaceResponse
{
    [self _readPlaceResponse];
    return _placeResponse != nil;
}
- (GEOPDPlaceResponse *)placeResponse
{
    [self _readPlaceResponse];
    return _placeResponse;
}
- (void)setPlaceResponse:(GEOPDPlaceResponse *)v
{
    _flags.read_placeResponse = 1;
    _flags.wrote_placeResponse = 1;
    _placeResponse = v;
}

- (void)_readSourceApplication {
    if (_flags.read_sourceApplication) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_sourceApplication) {
            static const uint32_t tags[] = {3, 0};
            GEORPPlaceInfoReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasSourceApplication
{
    [self _readSourceApplication];
    return _sourceApplication != nil;
}
- (NSString *)sourceApplication
{
    [self _readSourceApplication];
    return _sourceApplication;
}
- (void)setSourceApplication:(NSString *)v
{
    _flags.read_sourceApplication = 1;
    _flags.wrote_sourceApplication = 1;
    _sourceApplication = v;
}

- (void)_readSourceUrl {
    if (_flags.read_sourceUrl) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_sourceUrl) {
            static const uint32_t tags[] = {4, 0};
            GEORPPlaceInfoReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasSourceUrl
{
    [self _readSourceUrl];
    return _sourceUrl != nil;
}
- (NSString *)sourceUrl
{
    [self _readSourceUrl];
    return _sourceUrl;
}
- (void)setSourceUrl:(NSString *)v
{
    _flags.read_sourceUrl = 1;
    _flags.wrote_sourceUrl = 1;
    _sourceUrl = v;
}


- (NSString *)description
{
    return [NSString stringWithFormat:@"%@ %@", [super description], [self dictionaryRepresentation]];
}

- (NSDictionary *)dictionaryRepresentation
{
    [self readAll:YES];
    NSMutableDictionary *dict = [NSMutableDictionary dictionary];
    {
        GEOPDPlaceRequest * value = self.placeRequest;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"place_request"];
        }
    }

    {
        GEOPDPlaceResponse * value = self.placeResponse;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"place_response"];
        }
    }

    {
        NSString * value = self.sourceApplication;
        if (value) {
            [dict setObject:value forKey:@"source_application"];
        }
    }

    {
        NSString * value = self.sourceUrl;
        if (value) {
            [dict setObject:value forKey:@"source_url"];
        }
    }



    return dict;
}


#define TAG_PARSE_ALL_RECURSIVE     (UINT32_MAX)
#define TAG_PARSE_ALL_NON_RECURSIVE (UINT32_MAX-1)
#define TAG_PARSE_INITIAL           (UINT32_MAX-2)
#define TAG_UNKNOWN_FIELDS          (UINT32_MAX-3)

- (void)readAll:(BOOL)recursively {
    os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (reader == nil) return;
    @synchronized(reader) {
        static const uint32_t recursiveTag[] = {TAG_PARSE_ALL_RECURSIVE, 0};
        static const uint32_t nonRecursiveTag[] = {TAG_PARSE_ALL_NON_RECURSIVE, 0};
        GEORPPlaceInfoReadSpecified(self, reader, recursively ? recursiveTag : nonRecursiveTag);
    }
}
BOOL GEORPPlaceInfoReadAllFrom(__unsafe_unretained GEORPPlaceInfo *self, __unsafe_unretained PBDataReader *reader, BOOL recursively) {
    if (self->_readerMarkLength != 0) {
        [NSException raise:NSInvalidArgumentException format:@"GEORPPlaceInfoReadAllFrom can only be called once per object"];
        return NO;
    }

    if (reader.position > UINT32_MAX || reader.length > UINT32_MAX) {
        os_log_fault(OS_LOG_DEFAULT, "Cannot decode data with position/length > UINT32_MAX");
        return NO;
    }

    static const uint32_t recursiveTag[] = {TAG_PARSE_INITIAL, TAG_PARSE_ALL_RECURSIVE, 0};
    static const uint32_t initialTag[] = {TAG_PARSE_INITIAL, 0};

    @synchronized(reader) {
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = reader;
        os_unfair_lock_unlock(&(self->_readerLock));

        self->_readerMarkPos = (uint32_t)reader.position;
        self->_readerMarkLength = (uint32_t)reader.length;
        BOOL result = GEORPPlaceInfoReadSpecified(self, reader, recursively ? recursiveTag : initialTag);
        return result;
    }
}

BOOL GEORPPlaceInfoReadFrom(__unsafe_unretained GEORPPlaceInfo *self, __unsafe_unretained PBDataReader *reader) {
    @synchronized(reader) {
        return GEORPPlaceInfoReadAllFrom(self, reader, NO);
    }
}

BOOL GEORPPlaceInfoReadSpecified(__unsafe_unretained GEORPPlaceInfo *self, __unsafe_unretained PBDataReader *reader, const uint32_t *tags) {
    if (reader == nil) {
        return NO;
    }
    reader->_length = (NSUInteger)self->_readerMarkLength;
    [reader seekToOffset:(NSUInteger)self->_readerMarkPos];
    uint32_t readLazyCount = 0
        + self->_flags.read_placeRequest
        + self->_flags.read_placeResponse
        + self->_flags.read_sourceApplication
        + self->_flags.read_sourceUrl
        ;
    #define requiredLazyFieldCount 3 // objc_msg_parse_all_threshold = 0.8

    BOOL readAll = NO;
    __attribute__((unused)) BOOL recursively = NO;
    BOOL isInitial = NO;
    size_t tagsRemaining = 0;
    for (; tags[tagsRemaining] != 0; ++tagsRemaining) {
        switch (tags[tagsRemaining]) {
            case TAG_PARSE_ALL_RECURSIVE:
                recursively = YES;
                readAll = YES;
                break;
            case TAG_PARSE_ALL_NON_RECURSIVE:
                readAll = YES;
                break;
            case TAG_PARSE_INITIAL:
                isInitial = YES;
                break;
            default:
                ++readLazyCount;
                break;
            }
    }
    if (!readAll && readLazyCount >= requiredLazyFieldCount) {
        readAll = YES;
    }
    const BOOL stopWhenNoMoreTagsRemaining = !readAll;
    if (readAll || isInitial) tags = NULL;

    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }
        
        BOOL shouldParseTag = readAll;
        for (size_t i = 0; tags != NULL && !shouldParseTag && tags[i] != 0; ++i) {
            shouldParseTag = (tags[i] == tag);
        }

        switch (tag) {

            case 1 /* placeRequest */:
            {
                if (!shouldParseTag || self->_flags.read_placeRequest) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_placeRequest) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEOPDPlaceRequest *new_placeRequest = [[GEOPDPlaceRequest alloc] init];
                self->_placeRequest = new_placeRequest;
                PBDataReaderMark mark_placeRequest;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_placeRequest);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEOPDPlaceRequestReadAllFrom(new_placeRequest, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_placeRequest);

                --tagsRemaining;
            }
            break;
            case 2 /* placeResponse */:
            {
                if (!shouldParseTag || self->_flags.read_placeResponse) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_placeResponse) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEOPDPlaceResponse *new_placeResponse = [[GEOPDPlaceResponse alloc] init];
                self->_placeResponse = new_placeResponse;
                PBDataReaderMark mark_placeResponse;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_placeResponse);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEOPDPlaceResponseReadAllFrom(new_placeResponse, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_placeResponse);

                --tagsRemaining;
            }
            break;
            case 3 /* sourceApplication */:
            {
                if (!shouldParseTag || self->_flags.read_sourceApplication) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_sourceApplication) {
                        --tagsRemaining;
                    }
                    break;
                }
                NSString * new_sourceApplication = PBReaderReadString(reader);
                self->_sourceApplication = new_sourceApplication;

                --tagsRemaining;
            }
            break;
            case 4 /* sourceUrl */:
            {
                if (!shouldParseTag || self->_flags.read_sourceUrl) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_sourceUrl) {
                        --tagsRemaining;
                    }
                    break;
                }
                NSString * new_sourceUrl = PBReaderReadString(reader);
                self->_sourceUrl = new_sourceUrl;

                --tagsRemaining;
            }
            break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    return NO;
                break;
        }
        if (stopWhenNoMoreTagsRemaining && tagsRemaining == 0) {
            break;
        }
    }


    if (readAll) {
        if (recursively) {
            [self->_placeRequest readAll:YES];
            [self->_placeResponse readAll:YES];
        }
        self->_flags.read_placeRequest = YES;
        self->_flags.read_placeResponse = YES;
        self->_flags.read_sourceApplication = YES;
        self->_flags.read_sourceUrl = YES;
    } else {
        for (size_t i = 0; tags != NULL && tags[i] != 0; ++i) {
            switch (tags[i]) {
                case 1 /* placeRequest */:
                    self->_flags.read_placeRequest = YES;
                    break;
                case 2 /* placeResponse */:
                    self->_flags.read_placeResponse = YES;
                    break;
                case 3 /* sourceApplication */:
                    self->_flags.read_sourceApplication = YES;
                    break;
                case 4 /* sourceUrl */:
                    self->_flags.read_sourceUrl = YES;
                    break;
            }
        }
    }

    #undef requiredLazyFieldCount
    if (readAll) {
        // reader is __unsafe_unretained
        const BOOL hadError = PBReaderHasError(reader);
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = nil;
        os_unfair_lock_unlock(&(self->_readerLock));
        return !hadError;
    }
    return !PBReaderHasError(reader);
}

+ (BOOL)isValid:(__unsafe_unretained PBDataReader *)reader
{
    return GEORPPlaceInfoIsValid(reader);
}

BOOL GEORPPlaceInfoIsValid(__unsafe_unretained PBDataReader *reader) {
    PBDataReaderMark resetMark;
    resetMark.pos = reader.position;
    resetMark.length = reader.length;
    #define returnValue(X) do { \
        const BOOL v = X; \
        PBReaderRecallMark(reader, &resetMark); \
        return v; \
    } while(0)
    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }

        switch (tag) {

            case 1 /* placeRequest */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_placeRequest;
                    if (!PBReaderPlaceMark(reader, &mark_placeRequest))
                        returnValue(NO);
                    if (!GEOPDPlaceRequestIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_placeRequest);
                }
                break;
            case 2 /* placeResponse */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_placeResponse;
                    if (!PBReaderPlaceMark(reader, &mark_placeResponse))
                        returnValue(NO);
                    if (!GEOPDPlaceResponseIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_placeResponse);
                }
                break;
            case 3 /* sourceApplication */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
                break;
            case 4 /* sourceUrl */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
                break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
        }
    }
    returnValue(!PBReaderHasError(reader));
    #undef returnValue
}

- (BOOL)readFrom:(PBDataReader *)reader
{
    return GEORPPlaceInfoReadAllFrom(self, reader, NO);
}

- (void)writeTo:(PBDataWriter *)writer
{
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            if (!GEORPPlaceInfoIsDirty(self)) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                uint32_t length = (_readerMarkLength - _readerMarkPos);
                NSData *data = [reader readBytes:length];
                [writer writeData:data];
                return;
            }
        }
    }

    [self readAll:NO];

    /* placeRequest */
    {
        if (self->_placeRequest != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_placeRequest, 1);
        }
    }

    /* placeResponse */
    {
        if (self->_placeResponse != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_placeResponse, 2);
        }
    }

    /* sourceApplication */
    {
        if (self->_sourceApplication)
        {
            PBDataWriterWriteStringField(writer, self->_sourceApplication, 3);
        }
    }

    /* sourceUrl */
    {
        if (self->_sourceUrl)
        {
            PBDataWriterWriteStringField(writer, self->_sourceUrl, 4);
        }
    }
}

BOOL GEORPPlaceInfoIsDirty(__unsafe_unretained GEORPPlaceInfo *self)
{
    if (self == nil) {
        return NO;
    }
    
    BOOL dirty = NO
        || self->_flags.wrote_placeRequest
        || (self->_flags.read_placeRequest && GEOPDPlaceRequestIsDirty(self->_placeRequest))
        || self->_flags.wrote_placeResponse
        || (self->_flags.read_placeResponse && GEOPDPlaceResponseIsDirty(self->_placeResponse))
        || self->_flags.wrote_sourceApplication
        || self->_flags.read_sourceApplication
        || self->_flags.wrote_sourceUrl
        || self->_flags.read_sourceUrl
    ;


    return dirty;
}


- (void)copyTo:(GEORPPlaceInfo *)other
{
    [self readAll:NO];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    other->_reader = _reader;
    other->_readerMarkPos = _readerMarkPos;
    other->_readerMarkLength = _readerMarkLength;
    os_unfair_lock_unlock(&_readerLock);
    if (_placeRequest)
    {
        other.placeRequest = _placeRequest;
    }
    if (_placeResponse)
    {
        other.placeResponse = _placeResponse;
    }
    if (_sourceApplication)
    {
        other.sourceApplication = _sourceApplication;
    }
    if (_sourceUrl)
    {
        other.sourceUrl = _sourceUrl;
    }
}

- (id)copyWithZone:(NSZone *)zone
{
    GEORPPlaceInfo *copy = [[[self class] allocWithZone:zone] init];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            BOOL wroteAny = NO
                || _flags.wrote_placeRequest
                || _flags.wrote_placeResponse
                || _flags.wrote_sourceApplication
                || _flags.wrote_sourceUrl
            ;
            if (!wroteAny) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                GEORPPlaceInfoReadAllFrom(copy, reader, NO);
                return copy;
            }
            [self readAll:NO];
        }
    }
    copy->_placeRequest = [_placeRequest copyWithZone:zone];
    copy->_placeResponse = [_placeResponse copyWithZone:zone];
    copy->_sourceApplication = [_sourceApplication copyWithZone:zone];
    copy->_sourceUrl = [_sourceUrl copyWithZone:zone];
    return copy;
}


- (BOOL)isEqual:(id)object
{
    GEORPPlaceInfo *other = (GEORPPlaceInfo *)object;
    if (![other isMemberOfClass:[self class]]) {
        return NO;
    }
    [self readAll:YES];
    [other readAll:YES];


    return YES
    &&
    ((!self->_placeRequest && !other->_placeRequest) || [self->_placeRequest isEqual:other->_placeRequest])
    &&
    ((!self->_placeResponse && !other->_placeResponse) || [self->_placeResponse isEqual:other->_placeResponse])
    &&
    ((!self->_sourceApplication && !other->_sourceApplication) || [self->_sourceApplication isEqual:other->_sourceApplication])
    &&
    ((!self->_sourceUrl && !other->_sourceUrl) || [self->_sourceUrl isEqual:other->_sourceUrl])
    ;
}

- (NSUInteger)hash
{
    [self readAll:YES];
    NSUInteger value = 0;

    return value
    ^
    [self->_placeRequest hash]
    ^
    [self->_placeResponse hash]
    ^
    [self->_sourceApplication hash]
    ^
    [self->_sourceUrl hash]
    ;
}

- (void)mergeFrom:(GEORPPlaceInfo *)other
{
    [other readAll:NO];
    if (self->_placeRequest && other->_placeRequest)
    {
        [self->_placeRequest mergeFrom:other->_placeRequest];
    }
    else if (!self->_placeRequest && other->_placeRequest)
    {
        [self setPlaceRequest:other->_placeRequest];
    }
    if (self->_placeResponse && other->_placeResponse)
    {
        [self->_placeResponse mergeFrom:other->_placeResponse];
    }
    else if (!self->_placeResponse && other->_placeResponse)
    {
        [self setPlaceResponse:other->_placeResponse];
    }
    if (other->_sourceApplication)
    {
        [self setSourceApplication:other->_sourceApplication];
    }
    if (other->_sourceUrl)
    {
        [self setSourceUrl:other->_sourceUrl];
    }
}


@end


