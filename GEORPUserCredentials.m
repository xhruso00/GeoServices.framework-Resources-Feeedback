// This file was automatically generated by gyb
// DO NOT EDIT!
// Compiled from reportaproblem.proto

#import "GEORPUserCredentials.h"
#import <ProtocolBuffer/PBConstants.h>
#import <ProtocolBuffer/PBHashUtil.h>
#import <ProtocolBuffer/PBDataReader.h>
#import <os/lock_private.h>

#if !__has_feature(objc_arc)
# error This generated file depends on ARC but it is not enabled
#endif

@implementation GEORPUserCredentials

- (instancetype)init
{
    self = [super init];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (instancetype)initWithData:(NSData *)data
{
    self = [super initWithData:data];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (void)_readIcloudUserPersonId {
    if (_flags.read_icloudUserPersonId) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_icloudUserPersonId) {
            static const uint32_t tags[] = {1, 0};
            GEORPUserCredentialsReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasIcloudUserPersonId
{
    [self _readIcloudUserPersonId];
    return _icloudUserPersonId != nil;
}
- (NSString *)icloudUserPersonId
{
    [self _readIcloudUserPersonId];
    return _icloudUserPersonId;
}
- (void)setIcloudUserPersonId:(NSString *)v
{
    _flags.read_icloudUserPersonId = 1;
    _flags.wrote_icloudUserPersonId = 1;
    _icloudUserPersonId = v;
}

- (void)_readIcloudUserMapsAuthToken {
    if (_flags.read_icloudUserMapsAuthToken) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_icloudUserMapsAuthToken) {
            static const uint32_t tags[] = {2, 0};
            GEORPUserCredentialsReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasIcloudUserMapsAuthToken
{
    [self _readIcloudUserMapsAuthToken];
    return _icloudUserMapsAuthToken != nil;
}
- (NSString *)icloudUserMapsAuthToken
{
    [self _readIcloudUserMapsAuthToken];
    return _icloudUserMapsAuthToken;
}
- (void)setIcloudUserMapsAuthToken:(NSString *)v
{
    _flags.read_icloudUserMapsAuthToken = 1;
    _flags.wrote_icloudUserMapsAuthToken = 1;
    _icloudUserMapsAuthToken = v;
}


- (NSString *)description
{
    return [NSString stringWithFormat:@"%@ %@", [super description], [self dictionaryRepresentation]];
}

- (NSDictionary *)dictionaryRepresentation
{
    [self readAll:YES];
    NSMutableDictionary *dict = [NSMutableDictionary dictionary];
    {
        NSString * value = self.icloudUserPersonId;
        if (value) {
            [dict setObject:value forKey:@"icloud_user_person_id"];
        }
    }

    {
        NSString * value = self.icloudUserMapsAuthToken;
        if (value) {
            [dict setObject:value forKey:@"icloud_user_maps_auth_token"];
        }
    }



    return dict;
}


#define TAG_PARSE_ALL_RECURSIVE     (UINT32_MAX)
#define TAG_PARSE_ALL_NON_RECURSIVE (UINT32_MAX-1)
#define TAG_PARSE_INITIAL           (UINT32_MAX-2)
#define TAG_UNKNOWN_FIELDS          (UINT32_MAX-3)

- (void)readAll:(BOOL)recursively {
    os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (reader == nil) return;
    @synchronized(reader) {
        static const uint32_t recursiveTag[] = {TAG_PARSE_ALL_RECURSIVE, 0};
        static const uint32_t nonRecursiveTag[] = {TAG_PARSE_ALL_NON_RECURSIVE, 0};
        GEORPUserCredentialsReadSpecified(self, reader, recursively ? recursiveTag : nonRecursiveTag);
    }
}
BOOL GEORPUserCredentialsReadAllFrom(__unsafe_unretained GEORPUserCredentials *self, __unsafe_unretained PBDataReader *reader, BOOL recursively) {
    if (self->_readerMarkLength != 0) {
        [NSException raise:NSInvalidArgumentException format:@"GEORPUserCredentialsReadAllFrom can only be called once per object"];
        return NO;
    }

    if (reader.position > UINT32_MAX || reader.length > UINT32_MAX) {
        os_log_fault(OS_LOG_DEFAULT, "Cannot decode data with position/length > UINT32_MAX");
        return NO;
    }

    static const uint32_t recursiveTag[] = {TAG_PARSE_INITIAL, TAG_PARSE_ALL_RECURSIVE, 0};
    static const uint32_t initialTag[] = {TAG_PARSE_INITIAL, 0};

    @synchronized(reader) {
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = reader;
        os_unfair_lock_unlock(&(self->_readerLock));

        self->_readerMarkPos = (uint32_t)reader.position;
        self->_readerMarkLength = (uint32_t)reader.length;
        BOOL result = GEORPUserCredentialsReadSpecified(self, reader, recursively ? recursiveTag : initialTag);
        return result;
    }
}

BOOL GEORPUserCredentialsReadFrom(__unsafe_unretained GEORPUserCredentials *self, __unsafe_unretained PBDataReader *reader) {
    @synchronized(reader) {
        return GEORPUserCredentialsReadAllFrom(self, reader, NO);
    }
}

BOOL GEORPUserCredentialsReadSpecified(__unsafe_unretained GEORPUserCredentials *self, __unsafe_unretained PBDataReader *reader, const uint32_t *tags) {
    if (reader == nil) {
        return NO;
    }
    reader->_length = (NSUInteger)self->_readerMarkLength;
    [reader seekToOffset:(NSUInteger)self->_readerMarkPos];
    uint32_t readLazyCount = 0
        + self->_flags.read_icloudUserPersonId
        + self->_flags.read_icloudUserMapsAuthToken
        ;
    #define requiredLazyFieldCount 1 // objc_msg_parse_all_threshold = 0.8

    BOOL readAll = NO;
    __attribute__((unused)) BOOL recursively = NO;
    BOOL isInitial = NO;
    size_t tagsRemaining = 0;
    for (; tags[tagsRemaining] != 0; ++tagsRemaining) {
        switch (tags[tagsRemaining]) {
            case TAG_PARSE_ALL_RECURSIVE:
                recursively = YES;
                readAll = YES;
                break;
            case TAG_PARSE_ALL_NON_RECURSIVE:
                readAll = YES;
                break;
            case TAG_PARSE_INITIAL:
                isInitial = YES;
                break;
            default:
                ++readLazyCount;
                break;
            }
    }
    if (!readAll && readLazyCount >= requiredLazyFieldCount) {
        readAll = YES;
    }
    const BOOL stopWhenNoMoreTagsRemaining = !readAll;
    if (readAll || isInitial) tags = NULL;

    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }
        
        BOOL shouldParseTag = readAll;
        for (size_t i = 0; tags != NULL && !shouldParseTag && tags[i] != 0; ++i) {
            shouldParseTag = (tags[i] == tag);
        }

        switch (tag) {

            case 1 /* icloudUserPersonId */:
            {
                if (!shouldParseTag || self->_flags.read_icloudUserPersonId) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_icloudUserPersonId) {
                        --tagsRemaining;
                    }
                    break;
                }
                NSString * new_icloudUserPersonId = PBReaderReadString(reader);
                self->_icloudUserPersonId = new_icloudUserPersonId;

                --tagsRemaining;
            }
            break;
            case 2 /* icloudUserMapsAuthToken */:
            {
                if (!shouldParseTag || self->_flags.read_icloudUserMapsAuthToken) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_icloudUserMapsAuthToken) {
                        --tagsRemaining;
                    }
                    break;
                }
                NSString * new_icloudUserMapsAuthToken = PBReaderReadString(reader);
                self->_icloudUserMapsAuthToken = new_icloudUserMapsAuthToken;

                --tagsRemaining;
            }
            break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    return NO;
                break;
        }
        if (stopWhenNoMoreTagsRemaining && tagsRemaining == 0) {
            break;
        }
    }


    if (readAll) {
        if (recursively) {
        }
        self->_flags.read_icloudUserPersonId = YES;
        self->_flags.read_icloudUserMapsAuthToken = YES;
    } else {
        for (size_t i = 0; tags != NULL && tags[i] != 0; ++i) {
            switch (tags[i]) {
                case 1 /* icloudUserPersonId */:
                    self->_flags.read_icloudUserPersonId = YES;
                    break;
                case 2 /* icloudUserMapsAuthToken */:
                    self->_flags.read_icloudUserMapsAuthToken = YES;
                    break;
            }
        }
    }

    #undef requiredLazyFieldCount
    if (readAll) {
        // reader is __unsafe_unretained
        const BOOL hadError = PBReaderHasError(reader);
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = nil;
        os_unfair_lock_unlock(&(self->_readerLock));
        return !hadError;
    }
    return !PBReaderHasError(reader);
}

+ (BOOL)isValid:(__unsafe_unretained PBDataReader *)reader
{
    return GEORPUserCredentialsIsValid(reader);
}

BOOL GEORPUserCredentialsIsValid(__unsafe_unretained PBDataReader *reader) {
    PBDataReaderMark resetMark;
    resetMark.pos = reader.position;
    resetMark.length = reader.length;
    #define returnValue(X) do { \
        const BOOL v = X; \
        PBReaderRecallMark(reader, &resetMark); \
        return v; \
    } while(0)
    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }

        switch (tag) {

            case 1 /* icloudUserPersonId */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
                break;
            case 2 /* icloudUserMapsAuthToken */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
                break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
        }
    }
    returnValue(!PBReaderHasError(reader));
    #undef returnValue
}

- (BOOL)readFrom:(PBDataReader *)reader
{
    return GEORPUserCredentialsReadAllFrom(self, reader, NO);
}

- (void)writeTo:(PBDataWriter *)writer
{
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            if (!GEORPUserCredentialsIsDirty(self)) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                uint32_t length = (_readerMarkLength - _readerMarkPos);
                NSData *data = [reader readBytes:length];
                [writer writeData:data];
                return;
            }
        }
    }

    [self readAll:NO];

    /* icloudUserPersonId */
    {
        if (self->_icloudUserPersonId)
        {
            PBDataWriterWriteStringField(writer, self->_icloudUserPersonId, 1);
        }
    }

    /* icloudUserMapsAuthToken */
    {
        if (self->_icloudUserMapsAuthToken)
        {
            PBDataWriterWriteStringField(writer, self->_icloudUserMapsAuthToken, 2);
        }
    }
}

BOOL GEORPUserCredentialsIsDirty(__unsafe_unretained GEORPUserCredentials *self)
{
    if (self == nil) {
        return NO;
    }
    
    BOOL dirty = NO
        || self->_flags.wrote_icloudUserPersonId
        || self->_flags.read_icloudUserPersonId
        || self->_flags.wrote_icloudUserMapsAuthToken
        || self->_flags.read_icloudUserMapsAuthToken
    ;


    return dirty;
}


- (void)copyTo:(GEORPUserCredentials *)other
{
    [self readAll:NO];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    other->_reader = _reader;
    other->_readerMarkPos = _readerMarkPos;
    other->_readerMarkLength = _readerMarkLength;
    os_unfair_lock_unlock(&_readerLock);
    if (_icloudUserPersonId)
    {
        other.icloudUserPersonId = _icloudUserPersonId;
    }
    if (_icloudUserMapsAuthToken)
    {
        other.icloudUserMapsAuthToken = _icloudUserMapsAuthToken;
    }
}

- (id)copyWithZone:(NSZone *)zone
{
    GEORPUserCredentials *copy = [[[self class] allocWithZone:zone] init];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            BOOL wroteAny = NO
                || _flags.wrote_icloudUserPersonId
                || _flags.wrote_icloudUserMapsAuthToken
            ;
            if (!wroteAny) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                GEORPUserCredentialsReadAllFrom(copy, reader, NO);
                return copy;
            }
            [self readAll:NO];
        }
    }
    copy->_icloudUserPersonId = [_icloudUserPersonId copyWithZone:zone];
    copy->_icloudUserMapsAuthToken = [_icloudUserMapsAuthToken copyWithZone:zone];
    return copy;
}


- (BOOL)isEqual:(id)object
{
    GEORPUserCredentials *other = (GEORPUserCredentials *)object;
    if (![other isMemberOfClass:[self class]]) {
        return NO;
    }
    [self readAll:YES];
    [other readAll:YES];


    return YES
    &&
    ((!self->_icloudUserPersonId && !other->_icloudUserPersonId) || [self->_icloudUserPersonId isEqual:other->_icloudUserPersonId])
    &&
    ((!self->_icloudUserMapsAuthToken && !other->_icloudUserMapsAuthToken) || [self->_icloudUserMapsAuthToken isEqual:other->_icloudUserMapsAuthToken])
    ;
}

- (NSUInteger)hash
{
    [self readAll:YES];
    NSUInteger value = 0;

    return value
    ^
    [self->_icloudUserPersonId hash]
    ^
    [self->_icloudUserMapsAuthToken hash]
    ;
}

- (void)mergeFrom:(GEORPUserCredentials *)other
{
    [other readAll:NO];
    if (other->_icloudUserPersonId)
    {
        [self setIcloudUserPersonId:other->_icloudUserPersonId];
    }
    if (other->_icloudUserMapsAuthToken)
    {
        [self setIcloudUserMapsAuthToken:other->_icloudUserMapsAuthToken];
    }
}


@end


