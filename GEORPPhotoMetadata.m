// This file was automatically generated by gyb
// DO NOT EDIT!
// Compiled from community_feedback.proto

#import "GEORPPhotoMetadata.h"
#import <ProtocolBuffer/PBConstants.h>
#import <ProtocolBuffer/PBHashUtil.h>
#import <ProtocolBuffer/PBDataReader.h>
#import <os/lock_private.h>

#import <GeoServices/GEOLocation.h>

#if !__has_feature(objc_arc)
# error This generated file depends on ARC but it is not enabled
#endif

@implementation GEORPPhotoMetadata

- (instancetype)init
{
    self = [super init];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (instancetype)initWithData:(NSData *)data
{
    self = [super initWithData:data];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (void)_readClientImageUuid {
    if (_flags.read_clientImageUuid) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_clientImageUuid) {
            static const uint32_t tags[] = {1, 0};
            GEORPPhotoMetadataReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasClientImageUuid
{
    [self _readClientImageUuid];
    return _clientImageUuid != nil;
}
- (NSString *)clientImageUuid
{
    [self _readClientImageUuid];
    return _clientImageUuid;
}
- (void)setClientImageUuid:(NSString *)v
{
    _flags.read_clientImageUuid = 1;
    _flags.wrote_clientImageUuid = 1;
    _clientImageUuid = v;
}

- (void)_readImageDescription {
    if (_flags.read_imageDescription) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_imageDescription) {
            static const uint32_t tags[] = {2, 0};
            GEORPPhotoMetadataReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasImageDescription
{
    [self _readImageDescription];
    return _imageDescription != nil;
}
- (NSString *)imageDescription
{
    [self _readImageDescription];
    return _imageDescription;
}
- (void)setImageDescription:(NSString *)v
{
    _flags.read_imageDescription = 1;
    _flags.wrote_imageDescription = 1;
    _imageDescription = v;
}

- (void)_readGeotag {
    if (_flags.read_geotag) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_geotag) {
            static const uint32_t tags[] = {3, 0};
            GEORPPhotoMetadataReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasGeotag
{
    [self _readGeotag];
    return _geotag != nil;
}
- (GEOLocation *)geotag
{
    [self _readGeotag];
    return _geotag;
}
- (void)setGeotag:(GEOLocation *)v
{
    _flags.read_geotag = 1;
    _flags.wrote_geotag = 1;
    _geotag = v;
}


- (NSString *)description
{
    return [NSString stringWithFormat:@"%@ %@", [super description], [self dictionaryRepresentation]];
}

- (NSDictionary *)dictionaryRepresentation
{
    [self readAll:YES];
    NSMutableDictionary *dict = [NSMutableDictionary dictionary];
    {
        NSString * value = self.clientImageUuid;
        if (value) {
            [dict setObject:value forKey:@"client_image_uuid"];
        }
    }

    {
        NSString * value = self.imageDescription;
        if (value) {
            [dict setObject:value forKey:@"image_description"];
        }
    }

    {
        GEOLocation * value = self.geotag;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"geotag"];
        }
    }


    if (_unknownFields)
        [dict setObject:[_unknownFields dictionaryRepresentation] forKey:@"Unknown Fields"];

    return dict;
}


#define TAG_PARSE_ALL_RECURSIVE     (UINT32_MAX)
#define TAG_PARSE_ALL_NON_RECURSIVE (UINT32_MAX-1)
#define TAG_PARSE_INITIAL           (UINT32_MAX-2)
#define TAG_UNKNOWN_FIELDS          (UINT32_MAX-3)

- (void)readAll:(BOOL)recursively {
    os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (reader == nil) return;
    @synchronized(reader) {
        static const uint32_t recursiveTag[] = {TAG_PARSE_ALL_RECURSIVE, 0};
        static const uint32_t nonRecursiveTag[] = {TAG_PARSE_ALL_NON_RECURSIVE, 0};
        GEORPPhotoMetadataReadSpecified(self, reader, recursively ? recursiveTag : nonRecursiveTag);
    }
}
BOOL GEORPPhotoMetadataReadAllFrom(__unsafe_unretained GEORPPhotoMetadata *self, __unsafe_unretained PBDataReader *reader, BOOL recursively) {
    if (self->_readerMarkLength != 0) {
        [NSException raise:NSInvalidArgumentException format:@"GEORPPhotoMetadataReadAllFrom can only be called once per object"];
        return NO;
    }

    if (reader.position > UINT32_MAX || reader.length > UINT32_MAX) {
        os_log_fault(OS_LOG_DEFAULT, "Cannot decode data with position/length > UINT32_MAX");
        return NO;
    }

    static const uint32_t recursiveTag[] = {TAG_PARSE_INITIAL, TAG_PARSE_ALL_RECURSIVE, 0};
    static const uint32_t initialTag[] = {TAG_PARSE_INITIAL, 0};

    @synchronized(reader) {
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = reader;
        os_unfair_lock_unlock(&(self->_readerLock));

        self->_readerMarkPos = (uint32_t)reader.position;
        self->_readerMarkLength = (uint32_t)reader.length;
        BOOL result = GEORPPhotoMetadataReadSpecified(self, reader, recursively ? recursiveTag : initialTag);
        return result;
    }
}

BOOL GEORPPhotoMetadataReadFrom(__unsafe_unretained GEORPPhotoMetadata *self, __unsafe_unretained PBDataReader *reader) {
    @synchronized(reader) {
        return GEORPPhotoMetadataReadAllFrom(self, reader, NO);
    }
}

BOOL GEORPPhotoMetadataReadSpecified(__unsafe_unretained GEORPPhotoMetadata *self, __unsafe_unretained PBDataReader *reader, const uint32_t *tags) {
    if (reader == nil) {
        return NO;
    }
    reader->_length = (NSUInteger)self->_readerMarkLength;
    [reader seekToOffset:(NSUInteger)self->_readerMarkPos];
    uint32_t readLazyCount = 0
        + self->_flags.read_clientImageUuid
        + self->_flags.read_imageDescription
        + self->_flags.read_geotag
        + self->_flags.read_unknownFields
        ;
    #define requiredLazyFieldCount 2 // objc_msg_parse_all_threshold = 0.8

    BOOL readAll = NO;
    __attribute__((unused)) BOOL recursively = NO;
    BOOL shouldParseUnknown = NO;
    BOOL isInitial = NO;
    size_t tagsRemaining = 0;
    for (; tags[tagsRemaining] != 0; ++tagsRemaining) {
        switch (tags[tagsRemaining]) {
            case TAG_PARSE_ALL_RECURSIVE:
                recursively = YES;
                readAll = YES;
                shouldParseUnknown = YES;
                break;
            case TAG_PARSE_ALL_NON_RECURSIVE:
                readAll = YES;
                shouldParseUnknown = YES;
                break;
            case TAG_PARSE_INITIAL:
                isInitial = YES;
                break;
            case TAG_UNKNOWN_FIELDS:
                shouldParseUnknown = YES;
                ++readLazyCount;
                break;
            default:
                ++readLazyCount;
                break;
            }
    }
    if (!readAll && readLazyCount >= requiredLazyFieldCount) {
        readAll = YES;
        shouldParseUnknown = YES;
    }
    const BOOL stopWhenNoMoreTagsRemaining = !(readAll || shouldParseUnknown);
    if (readAll || isInitial) tags = NULL;

    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }
        
        BOOL shouldParseTag = readAll;
        for (size_t i = 0; tags != NULL && !shouldParseTag && tags[i] != 0; ++i) {
            shouldParseTag = (tags[i] == tag);
        }

        switch (tag) {

            case 1 /* clientImageUuid */:
            {
                if (!shouldParseTag || self->_flags.read_clientImageUuid) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_clientImageUuid) {
                        --tagsRemaining;
                    }
                    break;
                }
                NSString * new_clientImageUuid = PBReaderReadString(reader);
                self->_clientImageUuid = new_clientImageUuid;

                --tagsRemaining;
            }
            break;
            case 2 /* imageDescription */:
            {
                if (!shouldParseTag || self->_flags.read_imageDescription) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_imageDescription) {
                        --tagsRemaining;
                    }
                    break;
                }
                NSString * new_imageDescription = PBReaderReadString(reader);
                self->_imageDescription = new_imageDescription;

                --tagsRemaining;
            }
            break;
            case 3 /* geotag */:
            {
                if (!shouldParseTag || self->_flags.read_geotag) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_geotag) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEOLocation *new_geotag = [[GEOLocation alloc] init];
                self->_geotag = new_geotag;
                PBDataReaderMark mark_geotag;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_geotag);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEOLocationReadAllFrom(new_geotag, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_geotag);

                --tagsRemaining;
            }
            break;
            default:
                if (!shouldParseUnknown || self->_flags.read_unknownFields) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                if (!self->_unknownFields)
                    self->_unknownFields = [[PBUnknownFields alloc] init];
                if (!PBUnknownFieldAdd(self->_unknownFields, tag, aType, reader))
                    return NO;
                break;
        }
        if (stopWhenNoMoreTagsRemaining && tagsRemaining == 0) {
            break;
        }
    }

    if (shouldParseUnknown) {
        self->_flags.read_unknownFields = YES;
    }

    if (readAll) {
        if (recursively) {
            [self->_geotag readAll:YES];
        }
        self->_flags.read_clientImageUuid = YES;
        self->_flags.read_imageDescription = YES;
        self->_flags.read_geotag = YES;
    } else {
        for (size_t i = 0; tags != NULL && tags[i] != 0; ++i) {
            switch (tags[i]) {
                case 1 /* clientImageUuid */:
                    self->_flags.read_clientImageUuid = YES;
                    break;
                case 2 /* imageDescription */:
                    self->_flags.read_imageDescription = YES;
                    break;
                case 3 /* geotag */:
                    self->_flags.read_geotag = YES;
                    break;
            }
        }
    }

    #undef requiredLazyFieldCount
    if (readAll) {
        // reader is __unsafe_unretained
        const BOOL hadError = PBReaderHasError(reader);
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = nil;
        os_unfair_lock_unlock(&(self->_readerLock));
        return !hadError;
    }
    return !PBReaderHasError(reader);
}

+ (BOOL)isValid:(__unsafe_unretained PBDataReader *)reader
{
    return GEORPPhotoMetadataIsValid(reader);
}

BOOL GEORPPhotoMetadataIsValid(__unsafe_unretained PBDataReader *reader) {
    PBDataReaderMark resetMark;
    resetMark.pos = reader.position;
    resetMark.length = reader.length;
    #define returnValue(X) do { \
        const BOOL v = X; \
        PBReaderRecallMark(reader, &resetMark); \
        return v; \
    } while(0)
    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }

        switch (tag) {

            case 1 /* clientImageUuid */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
                break;
            case 2 /* imageDescription */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
                break;
            case 3 /* geotag */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_geotag;
                    if (!PBReaderPlaceMark(reader, &mark_geotag))
                        returnValue(NO);
                    if (!GEOLocationIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_geotag);
                }
                break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
        }
    }
    returnValue(!PBReaderHasError(reader));
    #undef returnValue
}

- (BOOL)readFrom:(PBDataReader *)reader
{
    return GEORPPhotoMetadataReadAllFrom(self, reader, NO);
}

- (void)writeTo:(PBDataWriter *)writer
{
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            if (!GEORPPhotoMetadataIsDirty(self)) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                uint32_t length = (_readerMarkLength - _readerMarkPos);
                NSData *data = [reader readBytes:length];
                [writer writeData:data];
                return;
            }
        }
    }

    [self readAll:NO];

    /* clientImageUuid */
    {
        if (self->_clientImageUuid)
        {
            PBDataWriterWriteStringField(writer, self->_clientImageUuid, 1);
        }
    }

    /* imageDescription */
    {
        if (self->_imageDescription)
        {
            PBDataWriterWriteStringField(writer, self->_imageDescription, 2);
        }
    }

    /* geotag */
    {
        if (self->_geotag != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_geotag, 3);
        }
    }
    /* Unknown Fields */
    {
        [_unknownFields writeTo:writer];
    }
}

BOOL GEORPPhotoMetadataIsDirty(__unsafe_unretained GEORPPhotoMetadata *self)
{
    if (self == nil) {
        return NO;
    }
    
    BOOL dirty = NO
        || self->_flags.wrote_clientImageUuid
        || self->_flags.read_clientImageUuid
        || self->_flags.wrote_imageDescription
        || self->_flags.read_imageDescription
        || self->_flags.wrote_geotag
        || self->_flags.read_geotag
        || self->_flags.wrote_unknownFields
    ;


    return dirty;
}


- (void)copyTo:(GEORPPhotoMetadata *)other
{
    [self readAll:NO];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    other->_reader = _reader;
    other->_readerMarkPos = _readerMarkPos;
    other->_readerMarkLength = _readerMarkLength;
    os_unfair_lock_unlock(&_readerLock);
    if (_clientImageUuid)
    {
        other.clientImageUuid = _clientImageUuid;
    }
    if (_imageDescription)
    {
        other.imageDescription = _imageDescription;
    }
    if (_geotag)
    {
        other.geotag = _geotag;
    }
}

- (id)copyWithZone:(NSZone *)zone
{
    GEORPPhotoMetadata *copy = [[[self class] allocWithZone:zone] init];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            BOOL wroteAny = NO
                || _flags.wrote_clientImageUuid
                || _flags.wrote_imageDescription
                || _flags.wrote_geotag
                || _flags.wrote_unknownFields
            ;
            if (!wroteAny) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                GEORPPhotoMetadataReadAllFrom(copy, reader, NO);
                return copy;
            }
            [self readAll:NO];
        }
    }
    copy->_clientImageUuid = [_clientImageUuid copyWithZone:zone];
    copy->_imageDescription = [_imageDescription copyWithZone:zone];
    copy->_geotag = [_geotag copyWithZone:zone];
    copy->_unknownFields = _unknownFields;
    return copy;
}


- (BOOL)isEqual:(id)object
{
    GEORPPhotoMetadata *other = (GEORPPhotoMetadata *)object;
    if (![other isMemberOfClass:[self class]]) {
        return NO;
    }
    [self readAll:YES];
    [other readAll:YES];


    return YES
    &&
    ((!self->_clientImageUuid && !other->_clientImageUuid) || [self->_clientImageUuid isEqual:other->_clientImageUuid])
    &&
    ((!self->_imageDescription && !other->_imageDescription) || [self->_imageDescription isEqual:other->_imageDescription])
    &&
    ((!self->_geotag && !other->_geotag) || [self->_geotag isEqual:other->_geotag])
    ;
}

- (NSUInteger)hash
{
    [self readAll:YES];
    NSUInteger value = 0;

    return value
    ^
    [self->_clientImageUuid hash]
    ^
    [self->_imageDescription hash]
    ^
    [self->_geotag hash]
    ;
}

- (void)mergeFrom:(GEORPPhotoMetadata *)other
{
    [other readAll:NO];
    if (other->_clientImageUuid)
    {
        [self setClientImageUuid:other->_clientImageUuid];
    }
    if (other->_imageDescription)
    {
        [self setImageDescription:other->_imageDescription];
    }
    if (self->_geotag && other->_geotag)
    {
        [self->_geotag mergeFrom:other->_geotag];
    }
    else if (!self->_geotag && other->_geotag)
    {
        [self setGeotag:other->_geotag];
    }
}

- (PBUnknownFields *)unknownFields
{
    if (_flags.read_unknownFields) return _unknownFields;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return _unknownFields;
    @synchronized(reader) {
        if (!_flags.read_unknownFields) {
            static const uint32_t tags[] = {TAG_UNKNOWN_FIELDS, 0};
            GEORPPhotoMetadataReadSpecified(self, reader, tags);
        }
    }
    return _unknownFields;
}
- (void)clearUnknownFields:(BOOL)recursively
{
    _flags.read_unknownFields = 1;
    _flags.wrote_unknownFields = 1;
    _unknownFields = nil;
    if (!recursively) {
        return;
    }
    [self readAll:NO];
    [_geotag clearUnknownFields:YES];
}

@end


