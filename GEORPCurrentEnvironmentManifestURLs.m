// This file was automatically generated by gyb
// DO NOT EDIT!
// Compiled from reportaproblem.proto

#import "GEORPCurrentEnvironmentManifestURLs.h"
#import <ProtocolBuffer/PBConstants.h>
#import <ProtocolBuffer/PBHashUtil.h>
#import <ProtocolBuffer/PBDataReader.h>
#import <os/lock_private.h>

#import <GeoServices/GEORPManifestURL.h>

#if !__has_feature(objc_arc)
# error This generated file depends on ARC but it is not enabled
#endif

@implementation GEORPCurrentEnvironmentManifestURLs

- (instancetype)init
{
    self = [super init];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (instancetype)initWithData:(NSData *)data
{
    self = [super initWithData:data];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (void)_readEnvironmentDisplayName {
    if (_flags.read_environmentDisplayName) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_environmentDisplayName) {
            static const uint32_t tags[] = {1, 0};
            GEORPCurrentEnvironmentManifestURLsReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasEnvironmentDisplayName
{
    [self _readEnvironmentDisplayName];
    return _environmentDisplayName != nil;
}
- (NSString *)environmentDisplayName
{
    [self _readEnvironmentDisplayName];
    return _environmentDisplayName;
}
- (void)setEnvironmentDisplayName:(NSString *)v
{
    _flags.read_environmentDisplayName = 1;
    _flags.wrote_environmentDisplayName = 1;
    _environmentDisplayName = v;
}

- (void)_readEnvironmentReleaseName {
    if (_flags.read_environmentReleaseName) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_environmentReleaseName) {
            static const uint32_t tags[] = {2, 0};
            GEORPCurrentEnvironmentManifestURLsReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasEnvironmentReleaseName
{
    [self _readEnvironmentReleaseName];
    return _environmentReleaseName != nil;
}
- (NSString *)environmentReleaseName
{
    [self _readEnvironmentReleaseName];
    return _environmentReleaseName;
}
- (void)setEnvironmentReleaseName:(NSString *)v
{
    _flags.read_environmentReleaseName = 1;
    _flags.wrote_environmentReleaseName = 1;
    _environmentReleaseName = v;
}

- (void)_readUrls {
    if (_flags.read_urls) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_urls) {
            static const uint32_t tags[] = {3, 0};
            GEORPCurrentEnvironmentManifestURLsReadSpecified(self, reader, tags);
        }
    }
}
- (NSMutableArray<GEORPManifestURL *> *)urls
{
    [self _readUrls];
    return _urls;
}
- (void)setUrls:(NSMutableArray<GEORPManifestURL *> *)v
{
    _flags.read_urls = 1;
    _flags.wrote_urls = 1;
    _urls = v;
}
- (void)clearUrls
{
    _flags.read_urls = 1;
    _flags.wrote_urls = 1;
    [_urls removeAllObjects];
}
- (void)addUrl:(GEORPManifestURL *)i
{
    [self _readUrls];
    [self _addNoFlagsUrl:i];
    _flags.read_urls = 1;
    _flags.wrote_urls = 1;
}
- (void)_addNoFlagsUrl:(GEORPManifestURL *)i
{
    if (!_urls)
    {
        _urls = [[NSMutableArray alloc] init];
    }
    [_urls addObject:i];
}
- (NSUInteger)urlsCount
{
    [self _readUrls];
    return [_urls count];
}
- (GEORPManifestURL *)urlAtIndex:(NSUInteger)idx
{
    [self _readUrls];
    return [_urls objectAtIndex:idx];
}
+ (Class)urlType
{
    return [GEORPManifestURL class];
}


- (NSString *)description
{
    return [NSString stringWithFormat:@"%@ %@", [super description], [self dictionaryRepresentation]];
}

- (NSDictionary *)dictionaryRepresentation
{
    [self readAll:YES];
    NSMutableDictionary *dict = [NSMutableDictionary dictionary];
    {
        NSString * value = self.environmentDisplayName;
        if (value) {
            [dict setObject:value forKey:@"environment_display_name"];
        }
    }

    {
        NSString * value = self.environmentReleaseName;
        if (value) {
            [dict setObject:value forKey:@"environment_release_name"];
        }
    }

    if ([self->_urls count])
    {
        NSMutableArray *urlsDictReprs = [[NSMutableArray alloc] initWithCapacity:[self->_urls count]];
        for (GEORPManifestURL * i_url in self->_urls)
        {
            [urlsDictReprs addObject:[i_url dictionaryRepresentation]];
        }
        [dict setObject:urlsDictReprs forKey:@"url"];
    }



    return dict;
}


#define TAG_PARSE_ALL_RECURSIVE     (UINT32_MAX)
#define TAG_PARSE_ALL_NON_RECURSIVE (UINT32_MAX-1)
#define TAG_PARSE_INITIAL           (UINT32_MAX-2)
#define TAG_UNKNOWN_FIELDS          (UINT32_MAX-3)

- (void)readAll:(BOOL)recursively {
    os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (reader == nil) return;
    @synchronized(reader) {
        static const uint32_t recursiveTag[] = {TAG_PARSE_ALL_RECURSIVE, 0};
        static const uint32_t nonRecursiveTag[] = {TAG_PARSE_ALL_NON_RECURSIVE, 0};
        GEORPCurrentEnvironmentManifestURLsReadSpecified(self, reader, recursively ? recursiveTag : nonRecursiveTag);
    }
}
BOOL GEORPCurrentEnvironmentManifestURLsReadAllFrom(__unsafe_unretained GEORPCurrentEnvironmentManifestURLs *self, __unsafe_unretained PBDataReader *reader, BOOL recursively) {
    if (self->_readerMarkLength != 0) {
        [NSException raise:NSInvalidArgumentException format:@"GEORPCurrentEnvironmentManifestURLsReadAllFrom can only be called once per object"];
        return NO;
    }

    if (reader.position > UINT32_MAX || reader.length > UINT32_MAX) {
        os_log_fault(OS_LOG_DEFAULT, "Cannot decode data with position/length > UINT32_MAX");
        return NO;
    }

    static const uint32_t recursiveTag[] = {TAG_PARSE_INITIAL, TAG_PARSE_ALL_RECURSIVE, 0};
    static const uint32_t initialTag[] = {TAG_PARSE_INITIAL, 0};

    @synchronized(reader) {
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = reader;
        os_unfair_lock_unlock(&(self->_readerLock));

        self->_readerMarkPos = (uint32_t)reader.position;
        self->_readerMarkLength = (uint32_t)reader.length;
        BOOL result = GEORPCurrentEnvironmentManifestURLsReadSpecified(self, reader, recursively ? recursiveTag : initialTag);
        return result;
    }
}

BOOL GEORPCurrentEnvironmentManifestURLsReadFrom(__unsafe_unretained GEORPCurrentEnvironmentManifestURLs *self, __unsafe_unretained PBDataReader *reader) {
    @synchronized(reader) {
        return GEORPCurrentEnvironmentManifestURLsReadAllFrom(self, reader, NO);
    }
}

BOOL GEORPCurrentEnvironmentManifestURLsReadSpecified(__unsafe_unretained GEORPCurrentEnvironmentManifestURLs *self, __unsafe_unretained PBDataReader *reader, const uint32_t *tags) {
    if (reader == nil) {
        return NO;
    }
    reader->_length = (NSUInteger)self->_readerMarkLength;
    [reader seekToOffset:(NSUInteger)self->_readerMarkPos];
    uint32_t readLazyCount = 0
        + self->_flags.read_environmentDisplayName
        + self->_flags.read_environmentReleaseName
        + self->_flags.read_urls
        ;
    #define requiredLazyFieldCount 2 // objc_msg_parse_all_threshold = 0.8

    BOOL readAll = NO;
    __attribute__((unused)) BOOL recursively = NO;
    BOOL isInitial = NO;
    size_t tagsRemaining = 0;
    for (; tags[tagsRemaining] != 0; ++tagsRemaining) {
        switch (tags[tagsRemaining]) {
            case TAG_PARSE_ALL_RECURSIVE:
                recursively = YES;
                readAll = YES;
                break;
            case TAG_PARSE_ALL_NON_RECURSIVE:
                readAll = YES;
                break;
            case TAG_PARSE_INITIAL:
                isInitial = YES;
                break;
            default:
                ++readLazyCount;
                break;
            }
    }
    if (!readAll && readLazyCount >= requiredLazyFieldCount) {
        readAll = YES;
    }
    const BOOL stopWhenNoMoreTagsRemaining = !readAll;
    if (readAll || isInitial) tags = NULL;

    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }
        
        BOOL shouldParseTag = readAll;
        for (size_t i = 0; tags != NULL && !shouldParseTag && tags[i] != 0; ++i) {
            shouldParseTag = (tags[i] == tag);
        }

        switch (tag) {

            case 1 /* environmentDisplayName */:
            {
                if (!shouldParseTag || self->_flags.read_environmentDisplayName) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_environmentDisplayName) {
                        --tagsRemaining;
                    }
                    break;
                }
                NSString * new_environmentDisplayName = PBReaderReadString(reader);
                self->_environmentDisplayName = new_environmentDisplayName;

                --tagsRemaining;
            }
            break;
            case 2 /* environmentReleaseName */:
            {
                if (!shouldParseTag || self->_flags.read_environmentReleaseName) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_environmentReleaseName) {
                        --tagsRemaining;
                    }
                    break;
                }
                NSString * new_environmentReleaseName = PBReaderReadString(reader);
                self->_environmentReleaseName = new_environmentReleaseName;

                --tagsRemaining;
            }
            break;
            case 3 /* urls */:
            {
                if (!shouldParseTag || self->_flags.read_urls) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    break;
                }
                GEORPManifestURL *new_url = [[GEORPManifestURL alloc] init];
                [self _addNoFlagsUrl:new_url];
                PBDataReaderMark mark_url;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_url);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEORPManifestURLReadAllFrom(new_url, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_url);

            }
            break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    return NO;
                break;
        }
        if (stopWhenNoMoreTagsRemaining && tagsRemaining == 0) {
            break;
        }
    }


    if (readAll) {
        if (recursively) {
            for (GEORPManifestURL * i in self->_urls)
                [i readAll:YES];
        }
        self->_flags.read_environmentDisplayName = YES;
        self->_flags.read_environmentReleaseName = YES;
        self->_flags.read_urls = YES;
    } else {
        for (size_t i = 0; tags != NULL && tags[i] != 0; ++i) {
            switch (tags[i]) {
                case 1 /* environmentDisplayName */:
                    self->_flags.read_environmentDisplayName = YES;
                    break;
                case 2 /* environmentReleaseName */:
                    self->_flags.read_environmentReleaseName = YES;
                    break;
                case 3 /* urls */:
                    self->_flags.read_urls = YES;
                    break;
            }
        }
    }

    #undef requiredLazyFieldCount
    if (readAll) {
        // reader is __unsafe_unretained
        const BOOL hadError = PBReaderHasError(reader);
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = nil;
        os_unfair_lock_unlock(&(self->_readerLock));
        return !hadError;
    }
    return !PBReaderHasError(reader);
}

+ (BOOL)isValid:(__unsafe_unretained PBDataReader *)reader
{
    return GEORPCurrentEnvironmentManifestURLsIsValid(reader);
}

BOOL GEORPCurrentEnvironmentManifestURLsIsValid(__unsafe_unretained PBDataReader *reader) {
    PBDataReaderMark resetMark;
    resetMark.pos = reader.position;
    resetMark.length = reader.length;
    #define returnValue(X) do { \
        const BOOL v = X; \
        PBReaderRecallMark(reader, &resetMark); \
        return v; \
    } while(0)
    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }

        switch (tag) {

            case 1 /* environmentDisplayName */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
                break;
            case 2 /* environmentReleaseName */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
                break;
            case 3 /* urls */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_url;
                    if (!PBReaderPlaceMark(reader, &mark_url))
                        returnValue(NO);
                    if (!GEORPManifestURLIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_url);
                }
                break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
        }
    }
    returnValue(!PBReaderHasError(reader));
    #undef returnValue
}

- (BOOL)readFrom:(PBDataReader *)reader
{
    return GEORPCurrentEnvironmentManifestURLsReadAllFrom(self, reader, NO);
}

- (void)writeTo:(PBDataWriter *)writer
{
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            if (!GEORPCurrentEnvironmentManifestURLsIsDirty(self)) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                uint32_t length = (_readerMarkLength - _readerMarkPos);
                NSData *data = [reader readBytes:length];
                [writer writeData:data];
                return;
            }
        }
    }

    [self readAll:NO];

    /* environmentDisplayName */
    {
        if (self->_environmentDisplayName)
        {
            PBDataWriterWriteStringField(writer, self->_environmentDisplayName, 1);
        }
    }

    /* environmentReleaseName */
    {
        if (self->_environmentReleaseName)
        {
            PBDataWriterWriteStringField(writer, self->_environmentReleaseName, 2);
        }
    }

    /* urls */
    {
        for (GEORPManifestURL * i_url in self->_urls)
        {
            PBDataWriterWriteSubmessage(writer, i_url, 3);
        }

    }
}

BOOL GEORPCurrentEnvironmentManifestURLsIsDirty(__unsafe_unretained GEORPCurrentEnvironmentManifestURLs *self)
{
    if (self == nil) {
        return NO;
    }
    
    BOOL dirty = NO
        || self->_flags.wrote_environmentDisplayName
        || self->_flags.read_environmentDisplayName
        || self->_flags.wrote_environmentReleaseName
        || self->_flags.read_environmentReleaseName
        || self->_flags.wrote_urls
    ;

    if (!dirty) {

            if (self->_flags.read_urls) {
                for (GEORPManifestURL *i in self->_urls) {
                    if (GEORPManifestURLIsDirty(i)) {
                        return YES;
                    }
                }
            }
    }

    return dirty;
}


- (void)copyTo:(GEORPCurrentEnvironmentManifestURLs *)other
{
    [self readAll:NO];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    other->_reader = _reader;
    other->_readerMarkPos = _readerMarkPos;
    other->_readerMarkLength = _readerMarkLength;
    os_unfair_lock_unlock(&_readerLock);
    if (_environmentDisplayName)
    {
        other.environmentDisplayName = _environmentDisplayName;
    }
    if (_environmentReleaseName)
    {
        other.environmentReleaseName = _environmentReleaseName;
    }
    if ([self urlsCount])
    {
        [other clearUrls];
        NSUInteger urlsCnt = [self urlsCount];
        for (NSUInteger i = 0; i < urlsCnt; i++)
        {
            [other addUrl:[self urlAtIndex:i]];
        }
    }
}

- (id)copyWithZone:(NSZone *)zone
{
    GEORPCurrentEnvironmentManifestURLs *copy = [[[self class] allocWithZone:zone] init];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            BOOL wroteAny = NO
                || _flags.wrote_environmentDisplayName
                || _flags.wrote_environmentReleaseName
                || _flags.wrote_urls
            ;
            if (!wroteAny) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                GEORPCurrentEnvironmentManifestURLsReadAllFrom(copy, reader, NO);
                return copy;
            }
            [self readAll:NO];
        }
    }
    copy->_environmentDisplayName = [_environmentDisplayName copyWithZone:zone];
    copy->_environmentReleaseName = [_environmentReleaseName copyWithZone:zone];
    for (GEORPManifestURL * v in _urls)
    {
        GEORPManifestURL * vCopy = [v copyWithZone:zone];
        [copy addUrl:vCopy];
    }
    return copy;
}


- (BOOL)isEqual:(id)object
{
    GEORPCurrentEnvironmentManifestURLs *other = (GEORPCurrentEnvironmentManifestURLs *)object;
    if (![other isMemberOfClass:[self class]]) {
        return NO;
    }
    [self readAll:YES];
    [other readAll:YES];


    return YES
    &&
    ((!self->_environmentDisplayName && !other->_environmentDisplayName) || [self->_environmentDisplayName isEqual:other->_environmentDisplayName])
    &&
    ((!self->_environmentReleaseName && !other->_environmentReleaseName) || [self->_environmentReleaseName isEqual:other->_environmentReleaseName])
    &&
    ((!self->_urls && !other->_urls) || [self->_urls isEqual:other->_urls])

    ;
}

- (NSUInteger)hash
{
    [self readAll:YES];
    NSUInteger value = 0;

    return value
    ^
    [self->_environmentDisplayName hash]
    ^
    [self->_environmentReleaseName hash]
    ^
    [self->_urls hash]
    ;
}

- (void)mergeFrom:(GEORPCurrentEnvironmentManifestURLs *)other
{
    [other readAll:NO];
    if (other->_environmentDisplayName)
    {
        [self setEnvironmentDisplayName:other->_environmentDisplayName];
    }
    if (other->_environmentReleaseName)
    {
        [self setEnvironmentReleaseName:other->_environmentReleaseName];
    }
    for (GEORPManifestURL * iter_urls in other->_urls)
    {
        [self addUrl:iter_urls];
    }
}


@end


