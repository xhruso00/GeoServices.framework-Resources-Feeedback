// This file was automatically generated by gyb
// DO NOT EDIT!
// Compiled from reportaproblem.proto

#import "GEORPAuxiliaryControl.h"
#import <ProtocolBuffer/PBConstants.h>
#import <ProtocolBuffer/PBHashUtil.h>
#import <ProtocolBuffer/PBDataReader.h>
#import <os/lock_private.h>

#import <GeoServices/GEORPCarPlayAuxiliaryControl.h>
#import <GeoServices/GEORPWatchAuxiliaryControl.h>

#if !__has_feature(objc_arc)
# error This generated file depends on ARC but it is not enabled
#endif

@implementation GEORPAuxiliaryControl

- (instancetype)init
{
    self = [super init];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (instancetype)initWithData:(NSData *)data
{
    self = [super initWithData:data];
    if (self == nil) {
        return nil;
    }

    _readerLock = OS_UNFAIR_LOCK_INIT;
    return self;
}

- (void)_readWatch {
    if (_flags.read_watch) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_watch) {
            static const uint32_t tags[] = {1, 0};
            GEORPAuxiliaryControlReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasWatch
{
    [self _readWatch];
    return _watch != nil;
}
- (GEORPWatchAuxiliaryControl *)watch
{
    [self _readWatch];
    return _watch;
}
- (void)setWatch:(GEORPWatchAuxiliaryControl *)v
{
    _flags.read_watch = 1;
    _flags.wrote_watch = 1;
    _watch = v;
}

- (void)_readCar {
    if (_flags.read_car) return;
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (!reader) return;
    @synchronized(reader) {
        if (!_flags.read_car) {
            static const uint32_t tags[] = {2, 0};
            GEORPAuxiliaryControlReadSpecified(self, reader, tags);
        }
    }
}
- (BOOL)hasCar
{
    [self _readCar];
    return _car != nil;
}
- (GEORPCarPlayAuxiliaryControl *)car
{
    [self _readCar];
    return _car;
}
- (void)setCar:(GEORPCarPlayAuxiliaryControl *)v
{
    _flags.read_car = 1;
    _flags.wrote_car = 1;
    _car = v;
}


- (NSString *)description
{
    return [NSString stringWithFormat:@"%@ %@", [super description], [self dictionaryRepresentation]];
}

- (NSDictionary *)dictionaryRepresentation
{
    [self readAll:YES];
    NSMutableDictionary *dict = [NSMutableDictionary dictionary];
    {
        GEORPWatchAuxiliaryControl * value = self.watch;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"watch"];
        }
    }

    {
        GEORPCarPlayAuxiliaryControl * value = self.car;
        if (value) {
            [dict setObject:[value dictionaryRepresentation] forKey:@"car"];
        }
    }



    return dict;
}


#define TAG_PARSE_ALL_RECURSIVE     (UINT32_MAX)
#define TAG_PARSE_ALL_NON_RECURSIVE (UINT32_MAX-1)
#define TAG_PARSE_INITIAL           (UINT32_MAX-2)
#define TAG_UNKNOWN_FIELDS          (UINT32_MAX-3)

- (void)readAll:(BOOL)recursively {
    os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    if (reader == nil) return;
    @synchronized(reader) {
        static const uint32_t recursiveTag[] = {TAG_PARSE_ALL_RECURSIVE, 0};
        static const uint32_t nonRecursiveTag[] = {TAG_PARSE_ALL_NON_RECURSIVE, 0};
        GEORPAuxiliaryControlReadSpecified(self, reader, recursively ? recursiveTag : nonRecursiveTag);
    }
}
BOOL GEORPAuxiliaryControlReadAllFrom(__unsafe_unretained GEORPAuxiliaryControl *self, __unsafe_unretained PBDataReader *reader, BOOL recursively) {
    if (self->_readerMarkLength != 0) {
        [NSException raise:NSInvalidArgumentException format:@"GEORPAuxiliaryControlReadAllFrom can only be called once per object"];
        return NO;
    }

    if (reader.position > UINT32_MAX || reader.length > UINT32_MAX) {
        os_log_fault(OS_LOG_DEFAULT, "Cannot decode data with position/length > UINT32_MAX");
        return NO;
    }

    static const uint32_t recursiveTag[] = {TAG_PARSE_INITIAL, TAG_PARSE_ALL_RECURSIVE, 0};
    static const uint32_t initialTag[] = {TAG_PARSE_INITIAL, 0};

    @synchronized(reader) {
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = reader;
        os_unfair_lock_unlock(&(self->_readerLock));

        self->_readerMarkPos = (uint32_t)reader.position;
        self->_readerMarkLength = (uint32_t)reader.length;
        BOOL result = GEORPAuxiliaryControlReadSpecified(self, reader, recursively ? recursiveTag : initialTag);
        return result;
    }
}

BOOL GEORPAuxiliaryControlReadFrom(__unsafe_unretained GEORPAuxiliaryControl *self, __unsafe_unretained PBDataReader *reader) {
    @synchronized(reader) {
        return GEORPAuxiliaryControlReadAllFrom(self, reader, NO);
    }
}

BOOL GEORPAuxiliaryControlReadSpecified(__unsafe_unretained GEORPAuxiliaryControl *self, __unsafe_unretained PBDataReader *reader, const uint32_t *tags) {
    if (reader == nil) {
        return NO;
    }
    reader->_length = (NSUInteger)self->_readerMarkLength;
    [reader seekToOffset:(NSUInteger)self->_readerMarkPos];
    uint32_t readLazyCount = 0
        + self->_flags.read_watch
        + self->_flags.read_car
        ;
    #define requiredLazyFieldCount 1 // objc_msg_parse_all_threshold = 0.8

    BOOL readAll = NO;
    __attribute__((unused)) BOOL recursively = NO;
    BOOL isInitial = NO;
    size_t tagsRemaining = 0;
    for (; tags[tagsRemaining] != 0; ++tagsRemaining) {
        switch (tags[tagsRemaining]) {
            case TAG_PARSE_ALL_RECURSIVE:
                recursively = YES;
                readAll = YES;
                break;
            case TAG_PARSE_ALL_NON_RECURSIVE:
                readAll = YES;
                break;
            case TAG_PARSE_INITIAL:
                isInitial = YES;
                break;
            default:
                ++readLazyCount;
                break;
            }
    }
    if (!readAll && readLazyCount >= requiredLazyFieldCount) {
        readAll = YES;
    }
    const BOOL stopWhenNoMoreTagsRemaining = !readAll;
    if (readAll || isInitial) tags = NULL;

    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }
        
        BOOL shouldParseTag = readAll;
        for (size_t i = 0; tags != NULL && !shouldParseTag && tags[i] != 0; ++i) {
            shouldParseTag = (tags[i] == tag);
        }

        switch (tag) {

            case 1 /* watch */:
            {
                if (!shouldParseTag || self->_flags.read_watch) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_watch) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEORPWatchAuxiliaryControl *new_watch = [[GEORPWatchAuxiliaryControl alloc] init];
                self->_watch = new_watch;
                PBDataReaderMark mark_watch;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_watch);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEORPWatchAuxiliaryControlReadAllFrom(new_watch, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_watch);

                --tagsRemaining;
            }
            break;
            case 2 /* car */:
            {
                if (!shouldParseTag || self->_flags.read_car) {
                    if (!PBReaderSkipValueWithTag(reader, tag, aType))
                        return NO;
                    if (shouldParseTag && self->_flags.read_car) {
                        --tagsRemaining;
                    }
                    break;
                }
                GEORPCarPlayAuxiliaryControl *new_car = [[GEORPCarPlayAuxiliaryControl alloc] init];
                self->_car = new_car;
                PBDataReaderMark mark_car;
                BOOL markError = !PBReaderPlaceMark(reader, &mark_car);
                if (markError)
                {
                    return NO;
                }
                BOOL inError = !GEORPCarPlayAuxiliaryControlReadAllFrom(new_car, reader, recursively);
                if (inError)
                {
                    return NO;
                }
                PBReaderRecallMark(reader, &mark_car);

                --tagsRemaining;
            }
            break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    return NO;
                break;
        }
        if (stopWhenNoMoreTagsRemaining && tagsRemaining == 0) {
            break;
        }
    }


    if (readAll) {
        if (recursively) {
            [self->_watch readAll:YES];
            [self->_car readAll:YES];
        }
        self->_flags.read_watch = YES;
        self->_flags.read_car = YES;
    } else {
        for (size_t i = 0; tags != NULL && tags[i] != 0; ++i) {
            switch (tags[i]) {
                case 1 /* watch */:
                    self->_flags.read_watch = YES;
                    break;
                case 2 /* car */:
                    self->_flags.read_car = YES;
                    break;
            }
        }
    }

    #undef requiredLazyFieldCount
    if (readAll) {
        // reader is __unsafe_unretained
        const BOOL hadError = PBReaderHasError(reader);
        os_unfair_lock_lock_with_options(&(self->_readerLock), OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
        self->_reader = nil;
        os_unfair_lock_unlock(&(self->_readerLock));
        return !hadError;
    }
    return !PBReaderHasError(reader);
}

+ (BOOL)isValid:(__unsafe_unretained PBDataReader *)reader
{
    return GEORPAuxiliaryControlIsValid(reader);
}

BOOL GEORPAuxiliaryControlIsValid(__unsafe_unretained PBDataReader *reader) {
    PBDataReaderMark resetMark;
    resetMark.pos = reader.position;
    resetMark.length = reader.length;
    #define returnValue(X) do { \
        const BOOL v = X; \
        PBReaderRecallMark(reader, &resetMark); \
        return v; \
    } while(0)
    while (PBReaderHasMoreData(reader)) {
        uint32_t tag = 0;
        uint8_t aType = 0;
        PBReaderReadTag32AndType(reader, &tag, &aType);

        if (PBReaderHasError(reader))
            break;

        if (aType == TYPE_END_GROUP) {
            break;
        }

        switch (tag) {

            case 1 /* watch */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_watch;
                    if (!PBReaderPlaceMark(reader, &mark_watch))
                        returnValue(NO);
                    if (!GEORPWatchAuxiliaryControlIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_watch);
                }
                break;
            case 2 /* car */:
                if (TYPE_LENGTH_DELIMITED != aType)
                    returnValue(NO);
                {
                    PBDataReaderMark mark_car;
                    if (!PBReaderPlaceMark(reader, &mark_car))
                        returnValue(NO);
                    if (!GEORPCarPlayAuxiliaryControlIsValid(reader))
                        returnValue(NO);
                    PBReaderRecallMark(reader, &mark_car);
                }
                break;
            default:
                if (!PBReaderSkipValueWithTag(reader, tag, aType))
                    returnValue(NO);
                break;
        }
    }
    returnValue(!PBReaderHasError(reader));
    #undef returnValue
}

- (BOOL)readFrom:(PBDataReader *)reader
{
    return GEORPAuxiliaryControlReadAllFrom(self, reader, NO);
}

- (void)writeTo:(PBDataWriter *)writer
{
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            if (!GEORPAuxiliaryControlIsDirty(self)) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                uint32_t length = (_readerMarkLength - _readerMarkPos);
                NSData *data = [reader readBytes:length];
                [writer writeData:data];
                return;
            }
        }
    }

    [self readAll:NO];

    /* watch */
    {
        if (self->_watch != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_watch, 1);
        }
    }

    /* car */
    {
        if (self->_car != nil)
        {
            PBDataWriterWriteSubmessage(writer, self->_car, 2);
        }
    }
}

BOOL GEORPAuxiliaryControlIsDirty(__unsafe_unretained GEORPAuxiliaryControl *self)
{
    if (self == nil) {
        return NO;
    }
    
    BOOL dirty = NO
        || self->_flags.wrote_watch
        || (self->_flags.read_watch && GEORPWatchAuxiliaryControlIsDirty(self->_watch))
        || self->_flags.wrote_car
        || self->_flags.read_car
    ;


    return dirty;
}


- (void)copyTo:(GEORPAuxiliaryControl *)other
{
    [self readAll:NO];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    other->_reader = _reader;
    other->_readerMarkPos = _readerMarkPos;
    other->_readerMarkLength = _readerMarkLength;
    os_unfair_lock_unlock(&_readerLock);
    if (_watch)
    {
        other.watch = _watch;
    }
    if (_car)
    {
        other.car = _car;
    }
}

- (id)copyWithZone:(NSZone *)zone
{
    GEORPAuxiliaryControl *copy = [[[self class] allocWithZone:zone] init];
    os_unfair_lock_lock_with_options(&_readerLock, OS_UNFAIR_LOCK_DATA_SYNCHRONIZATION);
    PBDataReader *reader = _reader;
    os_unfair_lock_unlock(&_readerLock);
    @synchronized(reader) {
        if (reader) {
            BOOL wroteAny = NO
                || _flags.wrote_watch
                || _flags.wrote_car
            ;
            if (!wroteAny) {
                reader->_length = (NSUInteger)_readerMarkLength;
                [reader seekToOffset:(NSUInteger)_readerMarkPos];
                GEORPAuxiliaryControlReadAllFrom(copy, reader, NO);
                return copy;
            }
            [self readAll:NO];
        }
    }
    copy->_watch = [_watch copyWithZone:zone];
    copy->_car = [_car copyWithZone:zone];
    return copy;
}


- (BOOL)isEqual:(id)object
{
    GEORPAuxiliaryControl *other = (GEORPAuxiliaryControl *)object;
    if (![other isMemberOfClass:[self class]]) {
        return NO;
    }
    [self readAll:YES];
    [other readAll:YES];


    return YES
    &&
    ((!self->_watch && !other->_watch) || [self->_watch isEqual:other->_watch])
    &&
    ((!self->_car && !other->_car) || [self->_car isEqual:other->_car])
    ;
}

- (NSUInteger)hash
{
    [self readAll:YES];
    NSUInteger value = 0;

    return value
    ^
    [self->_watch hash]
    ^
    [self->_car hash]
    ;
}

- (void)mergeFrom:(GEORPAuxiliaryControl *)other
{
    [other readAll:NO];
    if (self->_watch && other->_watch)
    {
        [self->_watch mergeFrom:other->_watch];
    }
    else if (!self->_watch && other->_watch)
    {
        [self setWatch:other->_watch];
    }
    if (self->_car && other->_car)
    {
        [self->_car mergeFrom:other->_car];
    }
    else if (!self->_car && other->_car)
    {
        [self setCar:other->_car];
    }
}


@end


